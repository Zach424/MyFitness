name: myfitness-local

services:
  postgres:
    image: postgres:18.4-alpine
    environment:
      POSTGRES_DB: myfitness
      POSTGRES_PASSWORD: myfitness_local
      POSTGRES_USER: myfitness
    ports:
      - '127.0.0.1:54329:5432'
    volumes:
      - myfitness_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U myfitness -d myfitness']
      interval: 2s
      timeout: 3s
      retries: 30
      start_period: 5s

  redis:
    image: redis:8.8.0-alpine
    command:
      [
        'redis-server',
        '--save',
        '',
        '--appendonly',
        'no',
        '--maxmemory',
        '64mb',
        '--maxmemory-policy',
        'noeviction',
      ]
    ports:
      - '127.0.0.1:63799:6379'
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 2s
      timeout: 3s
      retries: 30
      start_period: 3s

  ai:
    build:
      context: ../../services/ai
    environment:
      AI_PROVIDER: fixture
      AI_SERVICE_TOKEN: myfitness-ai-local
    ports:
      - '127.0.0.1:8001:8001'
    healthcheck:
      test:
        [
          'CMD',
          'python',
          '-c',
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8001/health', timeout=2)",
        ]
      interval: 2s
      timeout: 3s
      retries: 30
      start_period: 5s

volumes:
  myfitness_postgres_data:
