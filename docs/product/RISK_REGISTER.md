# 产品风险登记册

最后审阅：2026-08-11，第 143 轮

本登记册跟踪会影响发布的不确定性。缓解措施代表需要收集的证据，不表示风险已经消失。

第 101 轮仅复核 R-009 的本地包体证据；第 102 轮新增 R-030，以记录主观恢复估计尚未完成真实纵向校准；第 103 轮新增 R-031，并按结果回看的实际 WeApp 交付成本重基线本地包体预算；第 104–105 轮复核 R-009/R-031，历史与独立结果页保持非因果。第 106 轮再次复核 R-009/R-031：用户确认体验与系统观察分开且不进入自动适应。第 107 轮复核 R-009：H5 拆出 Taro 组件异步块，WeApp 折叠 XML 格式空白，并把两个改善后的预算向下收紧；本地增长阻塞已缓解，但真实设备加载仍未证明。第 108 轮复核 R-009/R-030/R-031，并新增 R-032：个人状态账本固定区分本人确认、确认记录、系统观察和估计/Unknown。第 109 轮复核 R-009/R-032：四类卡片可返回既有计划、记录与历史权威，导航与返回刷新保持分离；WeApp 总量降至 1,108,121 字节并恢复 879 字节余量，但统一表面的完整性与真实设备理解仍未证明。第 110 轮复核 R-030/R-032：Dashboard 的未来发生事实被 SQL、纯投影和契约共同隔离，但启发式恢复模型、证据完整性、真实用户理解和双时态知识历史都没有因此得到证明。第 111 轮复核 R-029：健康/动作点序列已拒绝晚于参考时刻的绝对时间，但这不证明目标平台的 IANA 时区数据、`localDate` 映射或真实设备行为。第 112 轮再次复核 R-029：营养 90 日序列现在绑定服务器参考本地日并拒绝日期偏移。第 113 轮继续复核 R-029：历史日历 28 日序列由纯投影和契约共同绑定参考本地日。第 114 轮再次复核 R-029：Dashboard 响应日期已绑定参考时刻本地日，但真实 H5/微信平台时区数据和客户端发生时间转换仍未完成设备证明。第 115 轮复核 R-030/R-032：三个非营养洞察响应已固定 7/30/90 窗口身份，但启发式模型校准、证据完整性和用户理解没有因此得到证明。第 117 轮再次复核 R-029：动作/健康点的响应本地日现在由构造器推导并受共享契约逐点验证，空序列也拒绝无效时区；真实 H5/微信 `Intl` 数据仍未证明。第 118 轮未新增或关闭风险：点序列顺序门禁防止时间线和截断漂移，但不证明趋势、因果、模型校准或真实用户理解。第 119 轮同样未新增或关闭风险：UUID 唯一性防止重复证据，但不证明来源真实性、证据完整性或统计解释。第 120 轮仍未新增或关闭风险：首点摘要和规范单位一致性防止标签/量纲漂移，但不证明记录真实性、单位换算正确性、趋势意义或医疗结论。第 121 轮仍未新增或关闭风险：截断收据一致性防止不受支持的 `hasMore` 声明，但不证明来源查询完整、后续证据数量或统计意义。全表最近一次完整审阅仍是上述第 043 轮。

第 122 轮仍未新增或关闭风险：有限时刻门禁防止无效原始点静默消失，但不证明记录时间真实、客户端时区数据完整或趋势解释正确。全表最近一次完整审阅仍是上述第 043 轮。

第 123 轮仍未新增或关闭风险：来源窗口身份门禁防止重复或未知聚合被静默选取/忽略，但不证明窗口查询完整、数值有效、统计意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 124 轮仍未新增或关闭风险：来源数值门禁防止非有限值、精度损失和非法训练负值进入投影，但不证明聚合关系、记录真实性、健康正常范围、统计意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 125 轮仍未新增或关闭风险：来源与公开聚合关系门禁防止不可能的组数或健康统计组合，但不证明跨窗口完整性、记录真实性、健康正常范围、趋势意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 126 轮仍未新增或关闭风险：来源 UUID 门禁防止无效聚合标识进入排序、唯一性与公开投影，但不证明记录所有权、真实性、来源字段有效、跨窗口完整性或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 127 轮仍未新增或关闭风险：动作快照门禁防止无效文本、枚举和器械关系进入投影，但不证明动作定义正确、安全、适合用户、来源真实或趋势解释成立。全表最近一次完整审阅仍是上述第 043 轮。

第 128 轮仍未新增或关闭风险：健康来源凭据门禁防止无效单位代码、来源元数据、记录时区和未确认 AI 来源进入投影，但不证明指标—单位关系、数值换算、记录真实性、健康范围或趋势意义。全表最近一次完整审阅仍是上述第 043 轮。

第 129 轮仍未新增或关闭风险：指标单位门禁防止已知但语义不属于指标的单位进入投影，但不证明展示值与规范值换算一致、记录真实、健康范围、趋势意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 130 轮仍未新增或关闭风险：持久化精度换算门禁防止规范值与原展示值明显错配，但不证明来源行属于请求指标、原始记录真实、设备准确、健康范围、趋势意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 131 轮仍未新增或关闭风险：逐点指标身份防止同单位的其他健康指标被替代适配器混入投影，但不证明 90 日窗口与点截断收据一致、记录真实、设备准确、健康范围、趋势意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 132 轮仍未新增或关闭风险：90 日窗口与点截断收据门禁防止完整计数、有限前缀和 `hasMore` 互相矛盾，但不证明 7/30/90 窗口集合单调、记录真实、设备准确、健康范围、趋势意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 133 轮仍未新增或关闭风险：跨窗口集合单调门禁防止 7/30/90 日嵌套窗口出现不可能的计数、记录日或极值边界，但不证明动作洞察收据一致、原始记录真实、设备准确、健康范围、趋势意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 134 轮仍未新增或关闭风险：动作 90 日课次与点截断收据门禁防止完整课次计数、有限训练前缀和 `hasMore` 互相矛盾，但不证明动作跨窗口聚合单调、训练记录真实、动作执行安全、趋势意义、训练效果或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 135 轮仍未新增或关闭风险：动作跨窗口单调门禁防止 7/30/90 日嵌套窗口的课次、完成组与非负测量总计减少，但不证明单窗口零证据语义完整、训练记录真实、动作执行安全、趋势意义、训练效果或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 136 轮仍未新增或关闭风险：动作窗口零证据门禁防止零课次携带完成组或零完成组携带非零测量，但不证明完整点聚合与窗口总计一致、训练记录真实、动作执行安全、趋势意义、训练效果或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 137 轮仍未新增或关闭风险：动作未截断聚合收据防止完整训练点与窗口总计矛盾，并明确跳过截断前缀，但不证明健康统计收据一致、训练记录真实、动作执行安全、趋势意义、训练效果或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 138 轮仍未新增或关闭风险：健康未截断统计收据防止完整规范值点集合与窗口极值/平均矛盾，并明确跳过截断前缀，但不证明记录日收据一致、原始记录真实、设备准确、健康范围、趋势意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 139 轮仍未新增或关闭风险：健康未截断记录日收据防止完整点集合与 90 日本地日数量矛盾，并覆盖夏令时重复小时，但不证明 7/30 日短窗口子集一致、目标平台 IANA 数据、原始记录真实、设备准确、趋势意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 140 轮仍未新增或关闭风险：健康短窗口子集收据防止完整 90 日点集与 7/30 日计数、记录日或统计矛盾，并覆盖闭区间边界，但不证明动作短窗口子集、非 UTC 生产数据库会话、目标平台 IANA 数据、原始记录真实、设备准确、趋势意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 141 轮仍未新增或关闭风险：动作短窗口子集收据防止完整 90 日训练点集与 7/30 日课次、组次或测量聚合矛盾，并覆盖闭区间和逐点舍入边界，但不证明非 UTC 生产数据库会话的日间隔语义、训练记录真实、动作执行安全、训练效果、趋势意义或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 142 轮仍未新增或关闭风险：动作/健康 SQL 已改用显式小时区间，并在非 UTC PostgreSQL 会话中证明跨春季/秋季 DST 都与绝对运行时边界一致；这加强 R-029 的服务端缓解证据，但不证明目标 H5/微信平台 IANA 数据、客户端发生时间转换、真实设备行为或用户理解。全表最近一次完整审阅仍是上述第 043 轮。

第 143 轮全表复核结论：30 项开放风险全部保持开放，严重度为 20 项高、10 项中；本轮 0 项新增、0 项关闭、0 项升级、0 项降级，`R-018` 继续作为历史空缺保留。逐项核对当前事实、仓库证据、责任前提和关闭条件后，只修正 R-022 已完成的源码/环境准入证据、R-024 已具备但尚未用于真实新候选的资格验证能力，以及 R-029 的服务端时区措辞。真实账号、设备、用户、专家、数据许可、政策、备份责任、托管资源、发布候选和生产材料均无新的具名外部证据，因此不能由本地门禁代替，也不能据此关闭对应风险。

第 144 轮局部复核 R-009/R-013，未新增、关闭或调整风险等级：API 与客户端现在共享 50 MiB 精确字节上限，服务端在最终 UTF-8 字符串超限时于附件 `Buffer` 分配前返回固定 413 收据，客户端仍在落盘前独立验证；但完整数据库行、对象图与格式化字符串仍在 API 内存中生成，尚未完成封闭测试规模测量、流式加密归档、限时交付或真实微信设备证明，因此 R-013 保持开放。共享常量使 H5/WeApp 总量各增加 14 字节并仍通过原预算，WeApp 余量收窄为 865 字节，R-009 同样保持开放。第 143 轮全表审阅基线不变。

第 145 轮局部复核 R-009/R-013，未新增、关闭或调整风险等级：客户端现在只依 HTTP 413 状态展示固定 50 MiB 产品说明，不读取或展示错误正文，也不下载、保存、自动重试或推进删除前导出选择；但这没有改变 API 全量构建数据库行、对象图和格式化字符串的峰值内存，也没有提供异步归档或真实微信文件证明，因此 R-013 保持开放。新增产品文案和分支使 H5 总量增至 1,211,160 字节，WeApp 总量增至 1,108,452/1,109,000 字节，只余 548 字节，R-009 保持开放且客户端增长余量更紧迫。第 143 轮全表审阅基线不变。

第 146 轮局部复核 R-009，未新增、关闭或调整风险等级：十一种读取表面的相同失败判定和九种标准快照状态机改为共享纯函数，各页面的导出名、类型、文案、失败关闭权限和特殊续页阶段保持不变。H5 总量/入口/最大异步块降至 1,206,969/315,262/149,734 字节，WeApp 总量/vendor/最大页面降至 1,105,112/19,338/56,943 字节；H5 异步和 WeApp 总量预算分别收紧到 152,000 与 1,107,000 字节，当前余量为 2,266 与 1,888 字节。R-009 获得结构性缓解但仍保持开放，因为 H5 入口仍高于 webpack 建议值，真实浏览器网络、微信真机加载、缓存和分包行为仍未证明。第 143 轮全表审阅基线不变。

第 147 轮局部复核 R-013，未新增、关闭或调整风险等级：API 在取得完整数据库快照和照片元数据后、读取任何私有照片对象前，先用所有 `media: null` 的完整 v4 结构计算精确 UTF-8 下界；下界超出共享 50 MiB 时返回既有固定 413，不读取对象、不生成 base64 或附件 `Buffer`。真实 PostgreSQL/私有对象存储测试证明有实际照片时该路径的 `PhotoStorageService.read()` 为零次。数据库 `byte_size` 因对象可能缺失而不作为拒绝权威。完整数据库结果、非媒体对象图和下界字符串仍同步生成，正常导出仍生成最终字符串；规模测量、流式加密归档和限时交付没有完成，所以 R-013 只获局部缓解并保持开放。第 143 轮全表审阅基线不变。

第 148 轮再次局部复核 R-013，未新增、关闭或调整风险等级：初始完整下界通过后，API 现在按真实 Buffer 长度、精确 base64 字符数和规范 JSON 包装增量维护常数时间运行下界；第 k 个媒体使结果超限时只读取到 k，不为 k 生成 base64，也不读取 k+1。多对象真实 PostgreSQL/私有对象存储测试证明读取一次、编码零次。数据库 `byte_size` 继续不参与拒绝。越界当前 Buffer、完整数据库结果、非媒体对象图和初始下界仍同步驻留，合法请求仍生成全部 base64 与最终字符串；规模测量、流式加密归档和限时交付没有完成，所以 R-013 继续保持开放。第 143 轮全表审阅基线不变。

第 149 轮再次局部复核 R-013，未新增、关闭或调整风险等级：新增只允许回环 PostgreSQL 的正式导出路径测量命令和无标识聚合收据。无媒体合成夹具在 25,000/65,000 条时分别生成 18,904,658/49,168,658 字节，65,000 条达到上限的 93.78% 且 RSS/heap 前后快照分别增加约 349.16/315.98 MiB；70,000 条由既有固定 413 拒绝。进程高水位包含迁移、Nest 和测量全过程，快照受 GC 影响，本地夹具不代表生产容量或 SLA。结果证明同步路径接近上限时有显著内存压力；异步归档的密钥引用、下载授权、保留和删除责任仍未定义，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 150 轮再次局部复核 R-013，未新增、关闭或调整风险等级：共享最小收据、六状态转换和迁移 0029 现在固定所有者幂等意图、确定性加密对象键、非秘密密钥引用、摘要/大小、生成/下载期限及失败/处置责任。数据库触发器拒绝跳级、回滚与同状态证据替换，RESTRICT 外键阻止账号删除留下孤儿归档；真实 PostgreSQL 已覆盖这些拒绝。当前没有请求服务、执行器、归档对象、短期下载授权、保留扫描或 UI，也未减少同步路径内存，因此这只是异步迁移的保管先决条件，R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 151 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部事务预约服务现在锁定 active owner，并用 owner/幂等 UUID 唯一约束把并发同请求收敛到同一收据；同键不同哈希、跨 owner、非 active owner 与终态复活均失败关闭。对象键、归档 UUID 和一小时生成期限只由服务端产生，迁移 0030 与共享契约还拒绝超过 JavaScript 安全整数的大小。当前仍没有公开路由、执行器、应用层流式加密、归档对象、短期下载授权、保留/擦除任务或 UI，也未减少同步路径内存，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 152 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部 AES-256-GCM codec 现在按有界帧认证 owner/archive/v4/key-ref 上下文，以唯一 nonce 和认证 final/EOF 拒绝篡改、错配、截断与尾随；8 MiB 以上测试证明 codec 输出块与总量解耦，完成收据只在全流消费后发布密文大小/SHA-256。但当前数据库快照和 v4 JSON 仍完整驻留，且没有 KMS lease、对象流式上传、执行器、短期授权或擦除/到期任务，因此不能从 codec 测试推断整个导出链内存或可用性，R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 153 轮再次局部复核 R-013，未新增、关闭或调整风险等级：精确 v4 JSON 字节源现在按有界块处理转义与 Unicode，不分配最终完整格式化字符串或明文附件 `Buffer`；超过 8 MiB 的组合测试已直接流经认证加密并逐块解密核对明文摘要，完成收据只在整流消费后发布。但输入仍是完整 `PrivacyExport`，数据库结果、对象图和照片 base64 尚未增量产生，且没有 KMS lease、对象流式上传、执行器、短期授权或擦除/到期任务；这减少未来归档的一层确定性副本，不等于当前同步路径内存下降或异步功能可用，R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 154 轮再次局部复核 R-013，未新增、关闭或调整风险等级：私有对象 writer 现在按 5–64 MiB 有界部件串行消费未知总长密文，以每部件 SHA-256、最终 `If-None-Match: *` 和完整密文摘要/大小收据形成背压与条件保管边界；真实 MinIO 已证明 JSON→认证加密→两部件对象对账，以及既有对象竞争、首部件后源失败、主动取消均无错误对象或 multipart 残留。但 writer 尚未与归档数据库状态/租约协调，完整 `PrivacyExport` 仍驻留，也没有 KMS、短期授权或擦除/到期任务；对象链可写不等于异步功能可用，R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 155 轮再次局部复核 R-013，未新增、关闭或调整风险等级：数据库现在可把一个只读 `REPEATABLE READ` 事务的寿命绑定到异步源物理 EOF，首个健康记录集合按默认 25、最大 100 行 keyset 页面读取；真实 PostgreSQL 证明同毫秒微秒顺序、并发插入/更新隔离、所有者门禁、取消回滚和完成收据。游标只保留 UUID，完整时间元组由相同快照中的锚点子查询比较，避免 JavaScript 毫秒精度造成遗漏。但该源尚未接入 v4 JSON，其他集合、嵌套聚合与媒体仍完整驻留，批次上限也不等于单行字节上限；KMS、状态执行、授权与处置仍缺失，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 156 轮再次局部复核 R-013，未新增、关闭或调整风险等级：v4 JSON 编码器现在允许数组字段使用同步/异步懒节点，只有遍历到字段才拉取元素；健康记录 repeatable-read 行源已在真实 PostgreSQL 测试中直接进入字段完整 v4，37 字节块输出与 eager 序列化逐字节相同，数据库/JSON 完成收据对账，中途停止则两者共同拒绝并回滚。这第一次移除了一个真实数据库集合在异步 v4 路径中的数组副本，但其余集合、嵌套聚合和媒体仍完整驻留，单元素 JSONB 也没有独立字节上界，完整生产服务尚未切换；KMS、状态执行、授权与处置仍缺失，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 157 轮再次局部复核 R-013，未新增、关闭或调整风险等级：健康记录快照现在要求每个数据库 JSON payload 不超过 64 KiB。PostgreSQL 先用 `octet_length` 精确计量 UTF-8 文本，超限时只把实际字节数交给应用，不返回或解析 payload；固定错误不含记录 ID/内容，并由行源、数据库收据和外层 JSON 收据按同一根对象传播。真实异常 JSONB 测试证明敏感标记未进入 JSON 字节流，合法边界值与既有 keyset/取消语义不变。该门禁只限制首个集合的数据库→Node 交付，不限制数据库内部编码成本；其余集合、嵌套聚合和媒体仍未迁移，完整生产服务、KMS、状态执行、授权与处置仍缺失，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 158 轮再次局部复核 R-013，未新增、关闭或调整风险等级：健康修订新增独立 repeatable-read keyset 行源与迁移 0031 的 `(user_id, changed_at, revision, id)` 索引，复用每批 25/最大 100 行、64 KiB payload 和同根失败内核。真实 PostgreSQL 证明微秒/UUID 总序、三页读取、并发追加隔离、跨 owner 排除、41 字节块 eager/lazy 等价及异常修订内容不出库。该源提高纵向更正历史的有界能力，但健康记录和修订仍属于两个独立事务，尚不能同时组成同一事实时刻的完整 v4；其余集合/媒体、KMS、状态执行、授权与处置仍缺失，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 159 轮再次局部复核 R-013，未新增、关闭或调整风险等级：健康记录与修订现在由一个协调器在同一只读 repeatable-read 事务中按 v4 顺序延迟读取，只执行一次 active owner 校验；双字段结束后仍等待 JSON 根物理 EOF 才提交。真实 PostgreSQL 证明第一字段结束后的并发新增对第二字段不可见，双懒数组以 43 字节块与 eager v4 等价，1 字节块在字段间取消时第二源未启动且统一数据库/JSON 收据共享取消错误。这消除了两个首要纵向集合的跨快照裂缝和字段间事务悬挂风险，但其余十一个集合、嵌套聚合和媒体仍完整驻留，完整生产投影、KMS、状态执行、授权与处置仍缺失，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 160 轮再次局部复核 R-013，未新增、关闭或调整风险等级：同意事件新增 `(accepted_at,id)` 有界行源，并与健康记录、健康修订由描述驱动协调器在同一只读 repeatable-read 快照中严格按 v4 顺序交付；同步 v4 同意查询也补上 UUID 尾序，既有降序历史索引可反向扫描而无需新增迁移。真实 PostgreSQL 证明同时间同意 UUID 总序、跨 owner 排除、第一字段结束后的同意/健康并发新增对后续字段不可见，三懒数组以 47 字节块与 eager v4 等价，1 字节块在同意/健康之间取消时健康源未启动且统一数据库/JSON 收据共享取消错误。这把隐私同意证据与健康当前/修订事实固定到同一事实时刻，但其余十个顶层集合、嵌套聚合和媒体仍完整驻留，完整生产投影、KMS、状态执行、授权与处置仍缺失，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 161 轮再次局部复核 R-013，未新增、关闭或调整风险等级：同步 v4 workout 顶层排序补上 UUID 尾序，真实 PostgreSQL 证明同时间总序，动作、组和修订仍由父级唯一 position/position/revision 确定。更重要的是，现有 `createWorkoutSchema` 接受的 30 个动作 × 每动作 50 组关系图，在排除 history 后的单 workout JSON 仍超过 64 KiB；每条修订还保存完整聚合且修订数无上限。由此拒绝把 workouts 作为第四个简单行源或调高 payload 门禁，后续必须提供嵌套懒来源、全历史 keyset 索引和分层取消收据。同步路径仍完整聚合训练对象，本轮只修复顺序并取得结构风险证据，R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 162 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部 `PortableExportJsonSource` 现在递归允许任意深度数组使用私有懒节点，workout exercises/sets/history 单元测试证明到达前零拉取、29 字节块 eager/lazy 等价，以及首个 set 后取消时先关闭内层迭代器再取消同一 JSON 根。这补齐分层训练流的类型与取消链，但没有增加 owner-scoped 全历史数据库源、索引、分层收据或第四协调字段，同步 workout 仍完整聚合，合法 30×50 反例仍然成立。因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 163 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部 workout 会话头源现在以 `(started_at,created_at,id)` keyset 覆盖活动和软删除记录，只把 15 个标量按 64 KiB 门禁交给 Node；迁移 0032 提供 owner-scoped 非部分索引。真实 PostgreSQL 证明三元组总序、跨 owner 排除、打开快照后的并发新增不可见、实际 SQL 可命中新索引，以及取消错误在流与收据间一致。该头源没有 exercises/history，也未进入三字段协调器或公开 v4；动作、组、修订、分层收据和完整生产执行仍缺失，同步 workout 继续完整聚合。因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 164 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部 `createWorkoutExerciseLayerSnapshot()` 现在让全历史 workout 头和按父级 `position` 分页的九字段动作头共享一次 active-owner、只读 repeatable-read 根事务。每个动作子流必须恰好一次且完整消费，外层才能推进；私有边界后仍需显式 `complete()` 才提交，跳过、重复、提前停止与主动取消会先关闭活动子流，再回滚根事务并拒绝统一头/动作收据。真实 PostgreSQL 已证明软删除/跨 owner 排除、位置总序、当前及后续 workout 并发动作新增不可见、实际 SQL 可使用既有唯一位置索引和活动子流取消同根。sets、修订、完整 v4 连接、媒体、KMS、状态执行、授权和处置仍缺失，同步 workout 继续完整聚合。因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 165 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部 `createWorkoutSetLayerSnapshot()` 现在把全历史 workout 头、按 position 分页的九字段动作头和十一字段 set 放入一次 active-owner、只读 repeatable-read 根事务。workout、exercise、set 都必须恰好一次且完整消费，私有边界后仍需显式 `complete()`；跳过、重复、提前停止与主动取消会先关闭最深活动 set，再关闭 exercise 和根事务，并拒绝统一三段收据。真实 PostgreSQL 已证明软删除/跨 owner 排除、set 总序、当前及后续 exercise 并发 set 新增不可见、实际 SQL 可使用既有唯一 set 位置索引和最深层取消同根。训练修订、完整 v4 连接、媒体、KMS、状态执行、授权和处置仍缺失，同步 workout 继续完整聚合。因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 166 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部 `createWorkoutRevisionHeaderLayerSnapshot()` 现在让 workout→exercise→set 当前关系图和按 revision 分页的 `id,action,revision,changed_at` 修订头共享一次 active-owner、只读 repeatable-read 根事务。关系图必须先完整消费，history 才能启动；乱序、跳过、重复、提前停止与主动取消都会关闭最深活动来源、回滚根事务并拒绝统一四段收据。真实 PostgreSQL 已证明软删除/跨 owner 排除、revision 总序、根快照后的并发修订追加不可见、实际 SQL 可使用既有修订索引，以及活动 history 取消同根。不可变 `snapshot`、完整 v4 连接、媒体、KMS、状态执行、授权和处置仍缺失，同步 workout 继续完整聚合。因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 167 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部 `inspectWorkoutRevisionSnapshotShape()` 现在沿精确 active-owner/workout/revision 父链，在 PostgreSQL 内检查不可变 snapshot 的根身份、严格键集、对象数组、数量、position 唯一和根头/动作头/set 的 64 KiB 边界，只返回兼容分类、计数、字节数、顺序布尔与 `decomposable`，不返回 UUID 或训练正文。真实数据库证明扩展、旧版、混合、反序、未知、超限和跨 owner；合法 `[2,1]` position 数组仍可分解，未来必须以 JSON ordinality 保留原表示。该收据只证明安全分解前置条件，不交付 snapshot、不连接 history/v4，也未减少同步内存；媒体、KMS、状态执行、授权和处置仍缺失。因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 168 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部 `createWorkoutRevisionSnapshot()` 现在让单个通过 shape 的 revision 在一次 active-owner、只读 repeatable-read 事务中按根→exercise→set 递归交付。shape 补充父级范围 UUID 唯一；根/动作使用空数组骨架保留 JSONB 对象键序，页面用元素 UUID 恢复 ordinality，三层执行 64 KiB 门禁、一次性完整消费、显式提交和最深层取消。真实 PostgreSQL 证明合法反序快照与直接 JSONB 逐字节相同，重复 UUID、未知、跨 owner 和取消失败关闭。该来源尚未嵌入多 revision history 或公开 v4，数据库内部仍重复展开 JSONB，也未减少同步内存；媒体、KMS、状态执行、授权和处置仍缺失。因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 169 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部 `createWorkoutRevisionSnapshotLayerSnapshot()` 复用单 revision 嵌套节点，把当前关系图与全部 revision snapshot 放进一次 active-owner、只读 repeatable-read 根事务。关系图必须先完整 EOF，含 `snapshot: null` 的修订 JSONB 骨架再原地挂载快照；每条 snapshot 未完整结束时不能推进下一条。真实 PostgreSQL 证明两条完整 history 与同步结构逐字节相同、反序 position 保持 JSON ordinality、未知 shape 在正文前关闭；数据库替身证明一个根流和最深层取消。完整 workout JSON 适配、跨顶层协调、媒体、KMS、状态执行、授权和处置仍缺失，同步路径仍完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 170 轮再次局部复核 R-013，未新增、关闭或调整风险等级：审计同步 v4 后确认 PostgreSQL JSONB 的 workout 对象键序为 `history→exercises`。新的 JSON 序会话复用七层训练状态机，以数据库生成的空数组和 `snapshot: null` 骨架保留字段键位，Node 只原位挂载递归懒节点；真实 PostgreSQL 已证明完整 `workouts` 数组与同步 v4 逐字节相同，并证明活动不可变 set 取消时 JSON 与数据库收据共享最深层错误。这消除了完整 workout 在独立异步来源中的对象图聚合，但尚未把它与前三字段放入同一事务，且排在 workouts 前的 exercise/food catalog、后续集合、媒体、KMS、状态执行、限时授权和处置仍缺失；同步公开路径继续完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 171 轮再次局部复核 R-013，未新增、关闭或调整风险等级：`exerciseCatalog` 现作为第四字段与同意、健康当前和健康修订共享一次 active-owner、只读 repeatable-read 事务。同步投影只含 owner 自定义活动/归档条目，不含内置 starter；条目按 `(created_at,id)` keyset，history 按 revision 子流交付，迁移 0033 提供非部分 owner 索引。条目和修订分别执行 64 KiB 门禁，最深 history 取消优先关闭根事务。真实 PostgreSQL 证明前三字段后的并发目录新增不可见、跨 owner/starter 排除、索引命中及四字段 eager/lazy 逐字节相同。`foodCatalog`、workouts 同根协调、后续集合/媒体、KMS、状态执行、限时授权和处置仍缺失，公开同步路径继续完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 172 轮再次局部复核 R-013，未新增、关闭或调整风险等级：`foodCatalog` 现作为第五字段与同意、健康当前、健康修订和动作目录共享一次 active-owner、只读 repeatable-read 事务。同步投影只含 owner 自定义活动/归档食物，不含内置 starter 参考目录；条目按 `(created_at,id)` keyset，history 按 revision 子流交付，迁移 0034 提供非部分 owner 索引。条目和修订分别执行 64 KiB 门禁，最深 food history 取消优先关闭根事务。真实 PostgreSQL 证明前四字段后的并发食物新增不可见、跨 owner/starter 排除、索引命中及五字段 eager/lazy 逐字节相同。workouts 同根协调、后续集合/媒体、KMS、状态执行、限时授权和处置仍缺失，公开同步路径继续完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 173 轮再次局部复核 R-013，未新增、关闭或调整风险等级：完整七层 workouts 现作为第六字段复用五字段协调根已有的 `PoolClient` 和 active-owner 结论，不新开事务或复制状态机。原 JSONB `history→exercises` 键序、全部 keyset、64 KiB 单元素门禁与最深层取消保持不变，七段统计合并进统一收据。真实 PostgreSQL 证明前五字段后的并发训练新增不可见、47 字节块六字段 eager/lazy 逐字节相同，活动不可变 set 取消时 JSON 与数据库收据共享最深错误。`nutritionMeals`、后续集合/媒体、KMS、状态执行、限时授权和处置仍缺失，公开同步路径继续完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 174 轮再次局部复核 R-013，未新增、关闭或调整风险等级：同步餐食顶层顺序补为 `(occurred_at,created_at,id)`，迁移 0035 的非部分 owner 索引覆盖软删除保管读取并由真实查询计划命中。内部 `inspectNutritionMealShape()` 只返回计数、字节数和 snapshot items 形状布尔，不返回 owner/meal UUID 或餐食正文；真实 PostgreSQL 的契约上限 30-item、4-revision 夹具证明 history payload 总量已超过 64 KiB，而 meal 头、当前 items 总量及单 revision 分别低于门禁。这固定了必须采用 meal/items/history/revision snapshot items 分层来源的反例，但本轮尚未输出餐食正文或接入第七协调字段；后续集合/媒体、KMS、状态执行、限时授权和处置仍缺失，公开同步路径继续完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 175 轮再次局部复核 R-013，未新增、关闭或调整风险等级：内部 `createNutritionMealLayerSnapshot()` 现以一次 active-owner、只读 repeatable-read 事务按 meal→当前 items→history→revision snapshot items 分层输出完整 owner 餐食。顶层/关系/修订使用三元 keyset、position 与 revision 索引，snapshot items 保持 JSON ordinality；五类 payload 在 PostgreSQL 内分别执行 64 KiB 门禁。真实数据库证明并发新增不可见、完整物化值与同步 v4 逐字节相同、非法 snapshot 和超限叶不泄露正文、活动最深 item 取消回滚同一根。该会话仍是独立事务且没有递归 JSON 适配，尚未接入第七协调字段；后续集合/媒体、KMS、状态执行、限时授权和处置仍缺失，公开同步路径继续完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 176 轮再次局部复核 R-013，未新增、关闭或调整风险等级：`createPortableExportNutritionMealJsonSource()` 现在把独立餐食会话的当前 items、history 与 revision snapshot items 原位包装为私有递归懒数组，创建后不拉取正文，也不建立第二事务或完成权威。单元测试证明字段到达前零预取和 `items→history→snapshot.items` 顺序；真实 PostgreSQL 以 31 字节块证明完整 `nutritionMeals` 与同步 v4 逐字节相同，活动历史 item 早停时 JSON 与数据库收据共享同一最深错误。该来源仍未复用六字段协调根，不能证明跨字段同一事实时刻；后续集合/媒体、KMS、状态执行、限时授权和处置仍缺失，公开同步路径继续完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 177 轮再次局部复核 R-013，未新增、关闭或调整风险等级：完整 `nutritionMeals` 现作为第七字段复用六字段协调根已有的 `PoolClient` 和 active-owner 结论，不新开事务、不重复 owner 查询，也不复制四层餐食状态机。meal、当前 items、history、revision snapshot 根与 snapshot items 五段统计并入统一收据，递归 JSON 仍由既有适配器原位包装。真实 PostgreSQL 证明前六字段结束后的并发餐食新增不可见、53 字节块七字段 eager/lazy 逐字节相同，活动历史 item 取消时 JSON、协调与数据库收据共享最深错误。`nutritionFavorites`、后续集合/媒体、KMS、状态执行、限时授权和处置仍缺失，公开同步路径继续完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 178 轮再次局部复核 R-013，未新增、关闭或调整风险等级：当前 owner `nutritionFavorites` 现作为第八字段复用七字段协调根。审计确认收藏没有软删除或历史表，复合主键 `(user_id,food_key)` 已覆盖 owner 隔离、唯一总序和查询计划，无需迁移；同步十四字段对象按 `food_key` keyset 并逐行执行 64 KiB 门禁。真实 PostgreSQL 证明最大合法行低于门禁、跨 owner 排除、根后并发新增不可见、43 字节块八字段 eager/lazy 逐字节相同。普通数组提前结束现在也会抛出协调根错误，避免 JSON 层生成第二通用错误。weeklyPlans、AI/照片集合、媒体、KMS、状态执行、限时授权和处置仍缺失，公开同步路径继续完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 179 轮再次局部复核 R-013，未新增、关闭或调整风险等级：`inspectWeeklyPlanShape()` 在 active-owner、只读 repeatable-read 会话中只返回计划头/current payload/history/workout links/体验反思的计数、字节与结构布尔。真实 PostgreSQL 以共享 Schema 合法 7×8×6 计划证明当前 payload 与单 revision 均超过 64 KiB，四条 history 和 400 条同时间 closed links 的聚合也超过门禁；收据不含 owner/plan UUID 或正文。同步 link 顺序补为 `(linked_at,id)`，迁移 0036 的非部分 `(user_id,plan_id,linked_at,id)` 索引由实际计划命中。该证据拒绝把 weeklyPlans 作为普通第九字段，但本轮尚未输出或分层计划正文；AI/照片集合、媒体、KMS、状态执行、限时授权和处置仍缺失，公开同步路径继续完整组装，因此 R-013 保持中等级开放。第 143 轮全表审阅基线不变。

第 180 轮再次局部复核 R-013 与 R-030，未新增、关闭或调整风险等级：`subjective-recovery-state-v1` 的 factor label、state label、note 和单条 limitation 现在分别限制为 60、80、320、240 字符，不截断、不改变启发式窗口、阈值、置信或因果边界。含 148 条证据引用和最大多字节文本的合法 `planEvidence` 低于 64 KiB，异常超长持久值在共享契约处失败关闭；这只解除计划 evidence 叶节点的无界标量风险，不表示 weeklyPlans 已递归迁移，也不校准主观恢复模型。项目主线转向 Personal Model 后，R-030 的关闭条件仍是纵向数据、专家审阅、用户校准与结果回看，而不是放宽字符或导出门禁。第 143 轮全表审阅基线不变。

第 181 轮完成 Personal Model 专项架构审阅并新增 R-033，未关闭或调整既有风险：长期个人认知模型如果混淆本人确认、系统观察和待验证假设，可能把缺失记录、选择偏差、短期状态或相关性固化为稳定标签。设计因此固定八类认知、支持/反对/上下文证据、精确修订、置信收据、争议/失效状态和用户校准；首批只允许两个低歧义确定性场景，且不推断依从性、动机、偏好、因果或医疗结论。上述控制目前只是受门禁保护的设计，不是实现或真实用户理解证明。当前登记为 31 项开放风险，严重度为 21 项高、10 项中；第 143 轮全表审阅基线不变。

第 182 轮局部复核 R-033，未新增、关闭或调整风险等级：P1a 共享契约用三个严格联合把 Constraint/Behavior/Baseline 绑定到对应主题与用户确认/确定性来源，只开放建档目标和训练修订证据，复核所有者、引用唯一性、支持/反对/撤回计数及置信覆盖；Unknown 使用独立原因收据，`candidate`/`disputed` 不能进入决策输入，反馈事件不能提交 `confidenceDelta`。这些规则阻止首批传输对象表达因果画像或客户端置信提权，但还没有不可变 revision/review、数据库所有者隔离、来源删除传播、API 授权、客户端标签理解或真实纵向阈值证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 183 轮局部复核 R-033，未新增、关闭或调整风险等级：P1b 用完整不可变快照、精确前驱和动作锁定模型修订；反馈必须指向当前非终态 revision，revised/no-op 结果不允许客户端提权，temporary 有效期必须一致。低覆盖观察允许保存为 disputed 以尊重用户异议，但仍不能进入决策输入。Weekly Cognitive Review 只接受有数量上限的结构化卡片和精确条目修订引用，拒绝跨 owner、未来、重复引用与自由叙事事实。这些门禁仍只是内部契约，没有 PostgreSQL 所有者隔离、来源撤回传播、生成选择器、API 授权、客户端标签理解或真实纵向阈值证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 184 轮局部复核 R-033，未新增、关闭或调整风险等级：P2a 以 item 当前指针和完整 revision 快照建立最小 PostgreSQL 内核，复合 owner/item/subject 外键、精确前驱、延迟当前指针约束和不可变触发器阻止跨用户历史、跳级发布与直接改写；仓储通过 `FOR UPDATE` 和期望修订让同一前驱的并发追加只有一个成功，账户删除级联已实测。反馈型 revision 在独立反馈事件表出现前全部失败关闭。这降低了数据库所有者隔离与并发漂移风险，但来源撤回、证据/反馈/回顾持久化、公开授权、便携导出、纵向覆盖、专家内容审阅和真实用户标签理解仍未证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 185 轮局部复核 R-033，未新增、关闭或调整风险等级：P2b 把四选一用户动作、理由/备注、时限和 revised/no-op 收据写入追加式事件表；精确 current 触发器和双向延迟外键阻止过期目标、悬空 revised 事件或没有事件的用户 revision。仓储支持相同事件结果重放并拒绝事件身份换内容，且将 item 行锁与锁后 current 读取分离，关闭并发等待旧快照竞态。真实 PostgreSQL 已证明 no-op 不生成伪历史、同事件并发只有一条事件/修订、原始 SQL 旁路失败和账户删除级联。这降低了用户校准持久化风险，但证据/回顾关系、来源撤回传播、公开授权、便携导出、纵向覆盖、专家内容审阅和真实用户标签理解仍未证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 186 轮局部复核 R-033，未新增、关闭或调整风险等级：P2c 把完整 revision 快照中的有序 evidence 引用原子投影到不可变关系表，延迟双向门禁逐项核对 owner、顺序、精确 JSON、四类计数和总数；少写、多写、换序、直接改写与直接删除均失败关闭，账户删除级联已实测。这降低了快照与查询关系漂移风险，但该关系仍只是快照投影，不是来源真实性或当前资格证明。训练已有不可变修订，建档 `user_goals` 却只有可变现态，因此 `onboarding_goal_revision` 尚不能建立真实来源外键，来源更正/删除也不能安全传播为 withdrawn 与新模型修订。来源权威、回顾、公开授权、便携导出、纵向覆盖、专家内容审阅和真实用户标签理解仍未证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 187 轮局部复核 R-033，未新增、关闭或调整风险等级：onboarding goal 现在拥有稳定聚合 ID、与资料一致的 revision、严格完整快照和不可变前驱链；当前行与历史双侧延迟核对，直接改写/删除失败，账户删除级联和本人同步便携导出已实测。迁移无法恢复已经覆盖的旧值，因此 revision 大于 1 的旧账号只保存 `migration_checkpoint + checkpoint_only`，后续链继承该限制，不伪造完整历史。这消除了 goal 来源表缺失，但 evidence projection 尚未用复合外键验证 goal/workout owner、聚合与精确 revision，也没有把来源更正/删除传播为 withdrawn 和新模型修订。回顾、Personal Model 公开授权/导出、纵向覆盖、专家内容审阅和真实用户标签理解仍未证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 188 轮局部复核 R-033，未新增、关闭或调整风险等级：goal/workout evidence 现在通过生成式类型键和复合外键命中同 owner、稳定聚合与精确不可变修订，延迟门禁还核对来源时间、类型和新 revision 写入时的当前未删除资格。来源更正/删除会在原事务追加不可变 refresh request；下一模型 revision 必须携带同一旧来源的 withdrawn context 并形成 resolution，遗漏、错理由、伪解决、直接改写或账户残留均由真实 PostgreSQL 失败关闭。旧模型历史仍保留当时陈述，没有被当前资格反向覆盖。但请求账本不会自行决定新 claim、置信或状态，确定性 refresh 执行器、回顾、公开授权/模型导出、纵向阈值、专家内容审阅和真实用户标签理解仍未证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 189 轮局部复核 R-033，未新增、关闭或调整风险等级：首个 training availability 纯派生器只复制本人当前 goal 的可训练日与时长，并生成不受随机身份/执行时刻影响的语义指纹；owner 锁事务证明首次创建、相同事实 no-op、唯一 goal refresh 消费和并发单修订。新 claim 不继承旧 confirmed/uncertain，用户 disagree 继续 disputed，终态不复活，账户删除零残留。该结果只覆盖本人确认 Constraint，没有证明 workout 记录完整、纵向 Behavior/Baseline 阈值、Pattern/Hypothesis 内容、回顾、公开授权/导出、专家审阅或真实用户理解，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 190 轮局部复核 R-033，未新增、关闭或调整风险等级：recorded training frequency 只使用账户建立后最近至多 8 个完整本地周和当前未删除 workout revisions；无完整周与无训练分别返回 Unknown，不把缺失记录编码成零行为。低于 4 周或 6 次只形成 candidate，active 仍保留单窗口限制；来源更正/删除、最后证据耗尽、异议和迟到终态刷新均有纯函数与真实 PostgreSQL 证明。账户建立时刻只代表数据库可观察起点，仍不能证明现实训练覆盖、门槛校准或习惯稳定；当前单主题单 item 也无法在终态后创建新一代。因此 R-033 保持高风险开放，下一轮先补代际生命周期，再处理训练时长、回顾、公开授权/导出、专家审阅和真实用户理解。第 143 轮全表审阅基线不变。

第 191 轮局部复核 R-033，未新增、关闭或调整风险等级：Personal Model item 以 generation、直接前代和一次性退役构成同主题不可分叉 lineage，部分唯一索引只允许一个未退役当前代。旧代只有在终态、来源待办已清且同事务创建 generation+1 时才能退役；退役代拒绝 revision/feedback/refresh，来源修订与退役的双向提交门禁阻止并发悬空义务。两个派生器只在出现前代未用的新精确来源时接续，并发收敛和账户删除零残留已实测。这解决终态后新认识无法形成的问题，但代际尚无公开授权/分页/导出/用户解释，训练时长、长期阈值、回顾、专家审阅和真实用户理解仍未证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 192 轮局部复核 R-033，未新增、关闭或调整风险等级：recorded session duration Baseline 只读取最近至多八个完整本地周内当前未删除的 workout revision，要求开始与结束都落在窗口内，并仅纳入正数且不超过 1,440 分钟的 elapsed duration。固定 numeric median 与 nearest-rank quartiles、Unknown/candidate/active、来源修订撤回、证据耗尽失效、异议保持、终态新代际、并发收敛和账户删除均由纯函数与真实 PostgreSQL 证明。这完成了首批第三个严格 claim，但训练记录仍可能遗漏或不代表现实训练，时长也不等于能力、强度、质量、有效刺激或建议；公开当前代/lineage 读取、模型导出、回顾、专家审阅和真实用户理解仍未证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 193 轮局部复核 R-033，未新增、关闭或调整风险等级：`personal-model-current-subject-envelope-v1` 与内部 `getCurrentSubject` 现以 active owner 和精确 subject 单语句选择唯一未退役代及 current revision；空主题明确为 null，owner 缺失/停用、非法 subject、重复当前代、跨 owner 或残缺连接失败关闭。信封同时绑定 generation、直接前代、terminal 和 `retiredAt:null`，避免终态当前代被误当退役、旧代被误当当前事实。这降低了后续读取选择错误，但尚无 HTTP 授权错误隐藏、公开字段最小化、lineage/证据分页、模型导出、回顾、专家审阅或真实用户理解，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 194 轮局部复核 R-033，未新增、关闭或调整风险等级：`personal-model-current-subject-view-v1` 现从严格内部信封生成 owner-free 最小可见投影，只保留结构化 claim、状态/反馈、置信限制、证据计数/窗口和必要时间，排除 owner、前代、内部 revision/reference UUID、EvidenceReference 正文、指纹与策略字段。应用服务把 owner 不存在、disabled 和 deletion_pending 统一为 unavailable，同时让歧义与损坏继续失败关闭；真实 PostgreSQL 覆盖空主题、普通当前、终态当前和 generation+1。它仍没有 HTTP guard/status/no-store/OpenAPI、lineage/证据分页、模型导出、回顾、专家审阅或真实用户理解，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 195 轮局部复核 R-033，未新增、关闭或调整风险等级：首个当前主题 HTTP 由现有 Bearer guard 绑定 owner，只接受三个共享 subject 并只返回严格最小视图。空主题为 200 + null；无认证、非法 subject、authority 竞态和内部故障分别保持 401/400/无身份线索 404/无内部正文 500。路径级前置中间件让五种状态均 `private, no-store`，OpenAPI 与真实 PostgreSQL 证明参数、owner 隔离和敏感字段排除。这降低了错误枚举、缓存和跨 owner 泄露风险，但客户端读取权限/标签理解、lineage/证据分页、模型导出、回顾、专家审阅与真实用户研究仍未证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 196 轮局部复核 R-033，未新增、关闭或调整风险等级：客户端传输只接受共享 subject，无平台依赖核心严格解析响应并拒绝 subject 错配、未知字段与畸形内容；错误保持失败而不是空主题。页面内存模型在请求前显式 unread，启动后区分 initial-loading/ready/refreshing/initial-error/stale；代次收据拒绝迟到结果，刷新失败保留整份旧快照，subject 切换和卸载清除证据。本轮模块没有页面引用、持久缓存或轮询，双端产物未变化。这降低了空主题/失败混淆和竞态覆盖风险，但中性展示、反馈可达性、lineage/证据分页、模型导出、回顾、专家审阅和真实用户理解仍未证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 197 轮局部复核 R-033，未新增、关闭或调整风险等级：纯展示模型把本人提交安排与系统整理的已记录频次/时长分开，空主题明确不是零，频次/时长反复限定记录范围；candidate、异议和终态分别说明不得作为当前计划依据。卡片同时显示本人核对、资料覆盖/限制、合格/支持/冲突/撤回精确计数、窗口/截止时间和代次/修订，不生成百分比、完成率、能力评级、效果判断或建议。组件无 API、交互、页面引用或持久化，双端产物未变化。这降低了未接线文案把记录偏差固化为评价的风险，但页面状态闭环、反馈可达性、真实用户理解、lineage/证据分页、模型导出、回顾与专家审阅仍未证明，因此 R-033 保持高风险开放。第 143 轮全表审阅基线不变。

第 198 轮局部复核 R-009 与 R-033，未新增、关闭或调整风险等级：首个页面固定开放“已记录训练频次”，初始失败保持 Unknown，刷新失败保留并明确标记上次完整快照，卸载后的迟到结果失权；固定边界说明未开放主题、历史和反馈。轻量严格响应校验器消除约 562 KiB 的 H5 Zod 路由膨胀，并与权威 Schema 做空态、三主题和畸形夹具一致性核对。WeApp 新页面净增 41,469 字节后，总量门槛从 1,107,000 有证据地校准为 1,150,000，实测只余 3,419 字节，vendor/单页门槛不变。这降低了首次用户可见读取中的 Unknown 混淆和依赖泄漏，但双实现契约漂移、真实成功路径、用户理解、三主题切换、反馈、lineage/证据分页、模型导出、回顾与专家审阅仍未证明；R-009 保持中、R-033 保持高。第 143 轮全表审阅基线不变。

第 199 轮局部复核 R-009 与 R-033，未新增、关闭或调整风险等级：三个严格主题通过唯一 pressed 索引逐项读取，本人安排与记录观察分别显示来源/非评价边界；切换先取消旧失败焦点并清除旧主题状态，旧 subject/generation 回调不能提交。页面不批量预取、不并排显示、不缓存每主题快照，避免在客户端形成组合画像。三张说明卡曾让 WeApp 超预算 745 字节，最终以紧凑索引和当前说明减去重复信息，预算未提高；实测总量只余 526 字节。真实成功路径、用户理解、反馈、lineage/证据分页、模型导出、回顾与专家审阅仍未证明；R-009 保持中、R-033 保持高。第 143 轮全表审阅基线不变。

第 200 轮局部复核 R-009 与 R-033，未新增、关闭或调整风险等级：反馈 HTTP 只接受最小 event/choice/reason/note/有效期，Bearer 与路径精确 revision 在 active owner/item 行锁内生成 revised/no-op；同 event 响应丢失重放返回首次收据，换内容、过期/终态、跨 owner 与 authority 失效失败关闭。最小响应排除 owner、claim、证据、note/reason 和内部指纹，全部状态 no-store。客户端反馈仍不可达，用户理解与完整闭环未证明；contracts 变化使 WeApp 总量只余 265 字节，下一轮必须先结构性减量。R-009 保持中、R-033 保持高，第 143 轮全表审阅基线不变。

| ID    | 风险                                                                            | 等级 | 当前控制 / 下一门禁                                                                                                                                                                                                                       |
| ----- | ------------------------------------------------------------------------------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R-002 | 微信登录只有模拟证明；H5 OIDC 已通过浏览器/提供方替身证明，但尚未在共享环境验证 | 高   | 配置真实客户端与域名，验证托管环境中的精确回调行为，并在封闭测试前演练设备/浏览器登录                                                                                                                                                     |
| R-003 | 本地恢复账本擦除有效，但缺少生产备份和提供方保管责任                            | 高   | 自动化备份/保留，独立保留账本，演练共享恢复并审批提供方控制                                                                                                                                                                               |
| R-004 | 真实食物照片的质量、延迟、成本和提供方数据控制仍未知                            | 高   | 默认继续使用夹具；要求责任人批准金丝雀，完成法务/区域/保留审阅并设定阈值                                                                                                                                                                  |
| R-005 | 本地 MinIO 不能证明生产对象存储的数据保管责任                                   | 高   | 配置云存储桶/KMS/IAM/生命周期/版本控制/复制，并演练故障与恢复                                                                                                                                                                             |
| R-006 | 初始食物成分数据只是演示数据                                                    | 高   | 在封闭测试前选择有许可、本地化、版本化且具备来源标注的目录                                                                                                                                                                                |
| R-007 | 二十三项确定性 AI 用例不足以形成真实世界安全证据                                | 高   | v2 已覆盖受阻 Unicode/指令切片及精确理由；补充专家审阅的真实图像、同形异义字、多语言攻击、拒绝案例和阈值                                                                                                                                  |
| R-008 | 进程指标缺少集中抓取、告警投递和具名责任人                                      | 高   | 部署私有聚合、仪表板和呼叫通知，并在封闭测试前演练第 012 轮事件运行手册                                                                                                                                                                   |
| R-009 | H5 入口仍超过 webpack 建议警告值，双端包体余量仍需持续保护                      | 中   | H5 入口/最大异步块为 315,456/149,898 字节，距 152,000 异步预算还余 2,102 字节；WeApp 总量为 1,149,735/1,150,000 字节且只余 265 字节。新增页面能力前必须先取得结构性降幅，不得放宽门槛掩盖依赖、重复文案或装饰增长；封闭测试前仍须真机复核 |
| R-010 | 用户仍可能过度信任食物照片估算                                                  | 高   | 保留范围/来源/PROOF 处理、用户编辑和禁止自动写入；与目标用户及专家共同验证                                                                                                                                                                |
| R-011 | 已有同意/事件机制，但政策与备案审阅尚未完成                                     | 高   | 使实际保留/提供方行为与隐私文本及适用的中国发布要求一致                                                                                                                                                                                   |
| R-012 | PostgreSQL、对象存储和提供方跨越事务边界                                        | 中   | 已有持久发件箱/租约/重试/故障测试；补充共享告警、死信责任人和提供方证据                                                                                                                                                                   |
| R-013 | 同步 JSON 导出仍在 API 内存组装，且账号规模可能超过 50 MiB 交付上限             | 中   | 同步路径有精确分阶段 413；八字段已有同事务懒 v4；weeklyPlans 已有 shape/总序证据但正文未分层，仍须迁移计划、AI/照片集合与媒体，并连接 KMS、状态执行、限时授权和处置                                                                       |
| R-014 | 固定窗口限流和代理跳数信任尚未按流量/拓扑校准                                   | 中   | 开放流量前执行边界负载测试，并在共享环境验证精确的 `TRUST_PROXY_HOPS`                                                                                                                                                                     |
| R-015 | 生产审计仍保留 9 个 Taro 构建/开发链 moderate 公告                              | 中   | critical/high 已为零；通过受支持的 Taro 升级消除剩余构建链问题，并重跑完整依赖图、双端客户端与 E2E 证据                                                                                                                                   |
| R-016 | 企业操作员身份和访问治理尚未配置                                                | 高   | 选择 OIDC 租户/客户端和具名责任人；使用前演练双人复核的开通、再认证、停用、撤销和共享登录                                                                                                                                                 |
| R-017 | 管理员审计只在主数据库内不可变                                                  | 高   | 定义保留与责任人；导出到独立保护的存储，纳入恢复证据，并在真实访问前为写入/访问异常配置告警                                                                                                                                               |
| R-019 | 擦除账本/密钥丢失会使用户与提供方身份抑制失效                                   | 高   | 独立保留/复制账本并恢复 HMAC 密钥；实现双读版本轮换并演练丢失场景                                                                                                                                                                         |
| R-020 | 死信任务缺少具名告警和最小权限恢复服务                                          | 高   | 本地使用精确任务审计运行手册；在共享封闭测试前补充集中呼叫、责任人和安全恢复流程                                                                                                                                                          |
| R-021 | MVP 的已擦除身份抑制当前会阻止同一身份创建新账号                                | 高   | 确认产品/法律政策；如允许回归，封闭测试前必须提供显式的新账号同意流程，且不得重新连接已擦除数据                                                                                                                                           |
| R-022 | 发布与环境准入证明通过后，托管部署仍未得到验证                                  | 中   | 托管质量/镜像冒烟、不可变 Actions、精确标签/main/CI 及服务端、客户端、环境准入已通过；仍须取得获批账号、预算、域名、凭据、材料和具名责任人，开通托管资源、部署金丝雀并演练回滚                                                            |
| R-023 | 离线准入无法证明外部引用真实存在或确已批准                                      | 高   | 在受保护的变更流程中创建、解引用并批准材料包；绝不因本地校验器成功而单独授予凭据或流量                                                                                                                                                    |
| R-024 | 已发布的 `v0.1.0-rc.1` 没有 H5/WeApp 或源码资格资产                             | 中   | 仓库已具备新候选的源码、服务与客户端资格组装/验证能力，但尚未发布真实合格新候选；仍须配置获批客户端 API URL，发布新的不可变候选，再独立验证全部服务/客户端/资格资产                                                                       |
| R-025 | 一个收据持有者密钥会留在客户端应用存储中，直至显式移除                          | 中   | 通过 HTTP 保持掩码和 no-store；在封闭测试前审阅安全平台存储、共享设备行为及过期/移除政策                                                                                                                                                  |
| R-026 | 已审阅的 GitHub Action 固定引用可能老化或错过上游安全/兼容修复                  | 中   | Dependabot 每周提出更新；核对精确上游标签/源码，同时更新锁定和所有用法，并在合并前要求完整托管 CI                                                                                                                                         |
| R-027 | 最终用户 OIDC 租户、恢复、关联和提供方数据政策尚未获批                          | 高   | 选择租户/客户端与责任人；审阅区域/保留/恢复；保持跨提供方关联关闭，并在封闭测试前演练 JWKS 轮换与事件路径                                                                                                                                 |
| R-028 | 进度照片可能被过度解读，或形成长期敏感媒体保管责任                              | 高   | 保留仅采集检查/无身体评分、分离分析/保留同意与限定范围删除；要求真实用户研究、政策和托管保管证明                                                                                                                                          |
| R-029 | 客户端发生时间转换依赖平台 `Intl` 时区数据                                      | 中   | 间隙/重叠/未来时间、Dashboard/营养 90 日/历史日历 28 日/动作健康点本地日门禁已通过；动作/健康 SQL 已使用显式小时区间消除数据库会话 DST 漂移，但仍不能替代真实平台 IANA 证明；封闭测试前跨代表性时区验证受支持 H5 浏览器和真实微信设备     |
| R-030 | 主观恢复状态的窗口、阈值和滚动个人基线仍是启发式模型                            | 高   | 当前保留逐条证据、Unknown/低置信/冲突门槛且仅允许中等置信一致估计影响计划；发生时间上界可阻止未来记录污染，但不能校准模型，封闭测试前仍须用真实纵向数据、专家审阅和结果回看校准                                                           |
| R-031 | 固定七天计划结果窗口和用户主动记录存在选择偏差，可能被误解为计划效果            | 高   | 当前/历史只显示精确修订、时间窗、确认来源和撤销计数；四档用户体验单列为 `user_confirmed` 当前事实，仍不推断因果、依从性或自动适应；封闭测试前评估记录覆盖、窗口与选项理解                                                                 |
| R-032 | 个人状态证据账本可能被误解为完整、实时或统一的个人真相                          | 高   | 固定知识类别、权威来源、快照时间、固定窗口身份、无保证有效期和失效原因；来源入口、显式刷新与发生时间上界均不冒充完整或双时态知识历史。封闭测试前仍须用真实用户验证类别、遗漏和时效理解                                                    |
| R-033 | 长期个人认知模型可能把记录偏差、缺失、短期变化或相关性固化为稳定标签            | 高   | 三个首批 claim、同主题代际、三主题逐项读取与精确反馈认证 HTTP 已证明；仍须补客户端反馈闭环/真实用户理解、Pattern/Hypothesis、回顾、可解释导出和专家审阅                                                                                   |

已解决的实现缺陷继续记录在各自迭代档案中，不从历史删除。产品级风险只有在具名的发布证据存在后才能关闭。
