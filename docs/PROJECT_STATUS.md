# Project status

Last reviewed: 2026-08-05

Stage: internal alpha locally runnable; consent-receipt history now treats collapse, unmount and loss of parent read authority as request-generation boundaries. A late first-page success cannot replace a newer accepted page with empty history, and a late continuation failure cannot publish a hidden retry, raw service copy or focus movement. Reopening explicitly repeats only the interrupted operation and exact cursor while the previous promise may finish but cannot commit. The 320 px accessibility matrix now uses four component-owned type variables with an exact synthetic 2× override; this preserves Taro's viewport-managed root sizing, long facts wrap and content-driven controls remain keyboard-operable. Consent-history failure authority, portable-export validation and all prior recovery/read surfaces remain green. H5/WeApp totals now measure 2,802,297/1,066,047 bytes; H5 entry/largest async are 319,238/205,178, vendor is 19,338 and the largest WeApp page is 55,697 bytes. Only the H5 total gate was narrowly rebased to 2,805,000 bytes; all other budgets are unchanged. Zero critical/high production audit findings remain. Owner-operated cloud, domains, real providers, telemetry and policy work stay parked while portable-export operation lifecycle is audited next.

Primary release target: WeChat Mini Program + responsive H5

## Objective

MyFitness / 衡迹 turns body, training, nutrition, and recovery records into safe, explainable, user-editable daily actions and weekly plans. The product is a general fitness and lifestyle tool, not a medical diagnosis or treatment product.

## Module status

| Module                  | Status                           | Current evidence                                                         | Next gate                                   |
| ----------------------- | -------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------- |
| Product scope           | Done for MVP baseline            | `docs/product/PRODUCT_BRIEF.md`                                          | Validate with target-user interviews        |
| Delivery roadmap        | Done for planning baseline       | `docs/product/ROADMAP.md`                                                | Continue local gaps; keep external gates    |
| Design language         | Partial, eighty-one flows tested | Core flows + 320 px/large-text/keyboard proof + 114 reviewed screenshots | Screen readers and remaining state matrix   |
| Client: Mini Program/H5 | Partial, candidate paths local   | H5/WeApp measured budgets plus candidate identity/release contracts      | Real providers/domains/device/browser proof |
| Admin console           | Partial, local slice done        | OIDC BFF, exact lookup, role split and Evidence Rail exercised           | Select IdP, owner, retention and deployment |
| Business API            | Partial                          | Verified WeChat/OIDC identity plus self-contained OCI runtime            | Shared deployment and real credential proof |
| Domain rules            | Partial                          | Stable current and all user-facing revision-history cursor pages         | Evaluate adaptive-plan policy safely        |
| AI service              | Partial                          | Crash-safe runs + adversarial text/vision validators + 23 evals          | Expert corpus + approved provider canary    |
| Native App/devices      | Deferred                         | Phase-two decision                                                       | MVP retention gate reached                  |
| Privacy/compliance      | Partial, durable local proof     | Purpose-scoped photo consent/export/deletion + restore replay tested     | Production retention/provider/legal review  |
| Testing/observability   | Partial                          | 408 unit, 63 integration, 96 browser tests + client quality gate         | Centralize telemetry after account approval |
| Deployment              | Partial, source/admission ready  | Immutable actions + tag/main/CI + service/client/environment gates       | Approve dossier, provision and canary       |

Status vocabulary: `Done` means validated for the present stage, `Partial` means usable but missing a named gate, `Pending` means not implemented, and `Deferred` means intentionally outside the current release.

## Current architecture

- Taro 4 + React + TypeScript for Mini Program and H5.
- pnpm workspace with checked-in lockfile and a shared CSS/TypeScript design-token package.
- A dependency-free project-status mirror selects the most recent open Obsidian vault (or an explicit override), copies `docs/PROJECT_STATUS.md` byte-for-byte inside that vault and independently rejects stale or path-escaping targets. The repository copy remains authoritative.
- Parent-qualified pnpm security floors isolate the Taro client on Vite 6.4.3/webpack 5.104.1 and patched parser/glob dependencies, while Next admin 16.2.11 keeps PostCSS 8.5.19 and omits its unused old Sharp optional dependency. The API retains Sharp 0.35.3 and Vitest remains on Vite 8.1.5; critical/high production audit findings are zero and nine moderate Taro build-chain findings remain registered.
- Separate `dist-h5` and `dist-weapp` production roots prevent one platform build from deleting the other. A dependency-free `myfitness-client-quality/v1` verifier measures the index-derived H5 entrypoint, largest async route, WeApp vendor/page bundles and total trees; strict checked-in budgets run after both builds in CI and before immutable release assembly.
- Portable-export transport is a lazily loaded client adapter so the privacy-only file path does not duplicate validation code across ordinary route chunks. It reads the H5 Blob or WeApp temporary file, then a dependency-free verifier accepts only the current JSON media type, exact v4 envelope/collection topology, valid generation time/account UUID and at most 50 MiB before creating an H5 anchor or invoking WeApp persistent save. The success receipt exposes only schema version, generation time and byte length; export content/account identity never enters UI state, logs or application storage, and rejected H5 Blob URLs are revoked.
- Consent receipt history is a separate read-only projection over the append-oriented `consent_events` table. Its strict default-10/max-20 page exposes only receipt UUID, purpose, version, accepted time and optional revoked time; no current-status field, user identity, provider detail or health content is returned. One base64url cursor carries only schema version plus receipt UUID. The API resolves that UUID under the authenticated owner and compares `(accepted_at, id)` against the database anchor without JavaScript timestamp conversion, while a covering owner/time/UUID index binds the exact order. New head events do not disturb an issued continuation, and the client keeps accepted pages only in React memory.
- The history client separately models collapsed, initial-loading, ready, refreshing, continuing, initial-error and retained-stale phases. A failed first read cannot publish the server-confirmed empty state; failed refresh/continuation keeps the accepted items and cursor in page memory, and retry repeats the exact operation. Offline transport, 4xx refusal, 5xx service outage and unknown adapter failure have product-owned copy and one H5 focus target. History freshness never supplies or removes current consent mutation authority, and no polling, persistent cache, background replay or raw service message is added.
- Every consent-history request receives a monotonic generation. Collapse snapshots only its operation and optional cursor, invalidates the generation and clears hidden busy/failure state; explicit reopen issues a fresh request for that exact intent. Unmount and parent disablement also invalidate the generation. Success, failure, focus scheduling and final busy cleanup commit only while generation, open, mounted and enabled authority still match. Requests are not physically aborted, persisted or replayed in the background.
- Consent-history-owned text uses four bounded component variables, content-driven button line height and explicit long-token wrapping. At 320 × 844 with an exact synthetic 2× variable override, the initial error, retained ten-item prefix, long version/timestamp labels, footer actions and current revocation control fit the scroll viewport without horizontal overflow. Root-relative units are deliberately avoided because Taro controls the H5 root size for viewport conversion. Taro's shared activation guard proves pointer behavior in the standard flows and Space/Enter behavior in the large-text matrix; these are local Chromium semantics, not a VoiceOver/TalkBack, browser text-only zoom or physical WeChat-device claim.
- Critical Taro H5 controls use a shared explicit pointer/Enter/Space activation contract because Taro's custom button element does not inherit native keyboard activation from `role` and `tabIndex`. The adapter also emits explicit `aria-disabled` and blocks pointer/keyboard callbacks when unavailable because the custom element's bare `disabled` attribute is not browser-native disabled semantics. Lazy routes use stable element IDs and delayed H5-only focus after the router transition; dialogs focus the safe cancel action first and return to the trigger or a stable fallback. Status surfaces are polite and atomic, while reduced-motion selectors stay inside each page root so they cannot alter the Taro router container. WeApp compilation remains green, but DOM-focus behavior is claimed only for tested H5 browsers.
- Browser validation accepts an explicit isolated MyFitness API loopback port and forwards the same origin to the API process, H5 build and administrator BFF. The default remains `127.0.0.1:3100`; a collision with another local project no longer requires terminating that process or changing test source, while fixtures cannot be redirected to a remote service.
- Manual nutrition does not initialize the optional photo workflow. `pages/progress-photos/index?kind=food` owns consent, reservation/upload, private candidate recovery, review and deletion on the existing lazy private-photo route. Taro's opener `EventChannel` exists only for that navigation instance and carries only the successful confirmation response's catalog keys/integer grams back to the preserved meal draft; direct visits cannot confirm, and photos, preview paths, consent and unconfirmed candidates never enter URL state or the 24-hour draft vault.
- A dependency-free `myfitness-sensitive-draft/v1` vault protects meaningful unsaved workout, meal and health-record create/correction input for at most 24 hours. Fixed non-identifying keys hold version/kind/owner/time metadata plus one exact page-owned payload; missing scope, cross-owner, invalid, expired or above-96-KiB values are purged. Corrections add only aggregate ID and positive base revision. Restore reads the exact owner-visible current aggregate and proceeds only on an exact revision match, even when the target is outside the first list page; stale/deleted targets are discarded without submitting, while later races still fail through normal optimistic concurrency. Restore is explicit, and save/cancel/logout/erasure initiation clear drafts without touching the separately governed erasure receipt.
- Health-record, workout and meal create recovery treats a transport failure as an unknown result rather than a failed fact. One dependency-free presentation contract separates network ambiguity, retryable HTTP outage, server refusal and unexpected failure. Each editor retains its exact in-memory idempotency key only while the attempted payload remains unchanged; retry reaches the existing server-side user/key/request-hash guard and every payload mutation resets both recovery state and key. Correction requests never receive a create-idempotency promise, and no request is persisted or replayed in the background.
- A separate dependency-free workbench matrix classifies twenty operations across action/food definitions, food/progress photos and weekly-plan generation, decisions, links and AI explanation. Only definition/reservation create can explicitly retry the same in-memory key. Every non-idempotent, evidence-sensitive, destructive or provider-calling stage exposes read-side reconciliation first; server refusal terminates that attempt. Definition correction compares the exact current revision and submitted fields, archive absence proves only loss from active choice, photo-confirm absence never emits unknown draft inputs, and media-delete absence never claims physical byte deletion. Progress-photo upload accepts only the exact ready item and never replays a selected file. Weekly-plan generation reads the exact requested week; decisions require the expected next revision/status and modified selections. Link creation requires the complete plan/session/workout revision tuple; unlink absence proves only that the target is no longer active. AI recovery reads only the original owner/key run, never invokes the worker, and displays completion only after exact plan-revision validation. Food nutrient/reference, progress capture, plan substitution/link and explanation intent remain page-owned input; media paths, photos, requests and replay commands are neither persisted nor queued.
- A dependency-free occurrence resolver maps an explicit `YYYY-MM-DD HH:mm` plus IANA timezone into one offset instant by round-tripping candidate offsets through `Intl.DateTimeFormat`. DST gaps fail, repeated minutes require an explicit UTC-offset choice and future instants fail in both client and shared API contracts. Untouched corrections preserve their exact original seconds/milliseconds; repeats reset time, and workouts expose independently validated start/end fields.
- An authenticated read-only history projection generates exactly 28 requested-timezone local dates ending at its reference instant, then counts current confirmed health records and current non-deleted workouts/meals from the owning user's source rows. It excludes later facts, stores no rollup and recomputes correction/deletion immediately. The client accepts range/timezone/series/totals atomically: unread evidence uses an unverified map and em dashes, while a failed refresh retains one labeled page-memory snapshot with date selection and all backfill navigation frozen. Explicit retry restores authority without polling or persistent projection cache. Accepted blank days retain no-score/adherence semantics; bounded backfill intents accept only a valid date from the previous 90 local days plus a valid IANA timezone and deliberately remain incomplete until the user enters `HH:mm`.
- Current health/workout/meal collection routes return strict `{ items, nextCursor }` pages in occurrence-time, aggregate-creation-time and UUID descending order. Calls without query parameters preserve the former 100/50/50 caps, while editors request 20 and progressively append unseen UUIDs. A versioned base64url cursor stores only aggregate UUID/revision; the API resolves its immutable owner snapshot so anchor correction/deletion cannot erase the sort boundary. Exact current-resource reads conceal deleted/foreign targets and support off-page correction recovery. Migration 0026 aligns partial current-row indexes with the total order.
- The health-record ledger applies five read-authority phases to its first current-record page and continuation cursor. Initial failure cannot become an empty record book, a zero-entry trend or permission to save. Refresh failure retains one labeled page in memory while create, correction, history/continuation and deletion remain frozen; draft input, back navigation, progress-photo entry and the independently read long-term observation route stay available. A foreground refresh closes old history/delete contexts, exposes one focusable retry and adds no polling, persistent list cache or background replay.
- The workout recording surface applies the same phases to one composed workout-page/cursor and exercise-catalog snapshot. The two requests may run together but neither half is accepted unless both succeed. Initial failure cannot become an empty training log or action directory; refresh failure retains both labeled halves while save, repeat, correction, history/continuation, deletion and catalog reuse remain frozen. Draft input, the independently read exercise observation and the dedicated owner-action register remain available. No polling, persistent cache or background replay is added.
- The nutrition recording surface applies the same phases to one composed meal-page/cursor, favorite-list and food-catalog snapshot. All three requests may run together but none is accepted unless every response succeeds. Initial failure cannot become an empty meal ledger, zero source tabs or an empty picker; refresh failure retains three labeled counts while save, repeat, favorite mutation, correction, history/continuation, deletion and food reuse remain frozen. Draft input, photo proof, nutrition observation and the dedicated owner-food register remain available. No polling, persistent cache or background replay is added.
- The dedicated owner-food and owner-action registers independently apply five phases and four product-owned failure families to their mutable definition lists. Initial failure keeps count/list unknown and freezes create/correction/history/archive; refresh failure retains the exact accepted owner definitions in page memory under a labeled receipt while the same operations remain frozen. Successful-empty is the only route to an empty register. Foreground refresh/retry is guarded, initial H5 focus follows the read outcome after route transition and no polling, persistent catalog cache or mutation replay is added.
- Health, exercise and nutrition long-term observations share five read phases, four product-owned failure families and one lazy authority component. Health/exercise publish source choices, selected identity and corresponding insight only after the complete chain succeeds; nutrition accepts one 90-local-day projection. Initial failure cannot become empty/zero evidence; refresh or identity-selection failure retains the labeled accepted projection in page memory. Server-backed identity choices freeze while stale, but local time-window/nutrient controls remain available because they perform no request or write. Failed selection intent is retained only for one explicit retry; there is no polling, persistent trend cache, diagnosis, target, adherence or prescription.
- Food-proof and progress-photo private inventories share five read phases, four product-owned failure families and one lazy authority surface, but keep purpose-specific copy and actions. Initial failure cannot become an empty review/capture state; refresh failure retains the exact accepted owner list in page memory under a stale receipt. Media, comparison, consent/retention, confirmation and deletion actions freeze, while the local progress overlay remains usable. Reconciliation stays operation-specific, and no polling, persistent photo/path/request cache, selected-file persistence or replay is added.
- Per-aggregate health/workout/meal history routes use the same minimal cursor envelope with a revision-only server boundary, default to 20 and cap at 50. The API verifies route UUID, owner and exact anchor revision before `revision < boundary` continuation; clients open with 10 snapshots and explicitly load the older suffix. New head revisions do not disturb an issued continuation, deleted aggregates retain their owner audit trail and existing descending revision indexes require no migration.
- The three aggregate history sheets share one page-memory read authority with initial-loading, ready, continuing, initial-error and retained-stale phases. Opening preserves the requested aggregate while the first page is unknown; only a successful empty response may publish an empty audit state. Continuation failure keeps accepted newest-first revisions readable under a bounded frozen-cursor receipt, and a stable focused retry reissues only the failed read. Their optional focus boundary stores only an in-memory trigger ID: guarded pointer/Enter/Space opening focuses the safe close control, failure may supersede it with retry focus and Escape/explicit/scrim dismissal restores the exact trigger or ledger fallback. Programmatic parent close does not restore focus. Closing, parent-ledger refresh and unmount invalidate late responses; history never polls, persists a cache, advances a stale cursor, surfaces raw transport text or changes parent mutation authority.
- Health-record, workout and meal destructive confirmations share a separate in-memory dialog focus boundary. Stable row triggers use the guarded Taro activation contract, opening focuses the non-destructive cancel action, and Escape/explicit cancel restore the exact row. Submission disables cancel/Escape until the request settles; a failed delete returns focus to cancel, while success clears the removed row and focuses the stable ledger-refresh control because the initiating node no longer exists. This changes only H5 interaction state: expected revision headers, server soft deletion, immutable history and parent read-authority gates remain unchanged.
- Aggregate deletion uses a separate dependency-free recovery classifier and one shared page receipt. Network, retryable service and unknown adapter results require reconciliation; an explicit non-retryable server refusal terminates the attempt. The client retains only the target aggregate/revision in page memory, closes the modal and disables every delete trigger until the owner-authenticated exact read resolves. A concealed 404 proves only absence from the current owner-visible ledger and removes the local row without replay; an identical revision permits a later explicit reconfirmation, while any different revision replaces the row and invalidates the old intent. Read failure retains the receipt, and successful full-ledger refresh is an equivalent reconciliation. No request queue, persistent recovery state or automatic DELETE is added.
- Aggregate correction uses a separate dependency-free recovery contract over the existing exact health/workout/meal reads. Network, retryable service and unknown adapter results retain the target ID, base revision, exact submitted request and draft signature in page memory; an explicit non-retryable refusal terminates the attempt. The primary save action becomes read-only reconciliation while that signature and editing base still match. Only an advanced revision whose complete submitted projection matches is accepted as saved; the same revision keeps the input for a later explicit PUT, while a different revision refreshes the comparison base without replacing the draft. A concealed 404 removes the stale ledger row and freezes the correction until cancel so it cannot silently become a create. Draft mutation invalidates the old recovery synchronously at render, and no automatic PUT, persistent request or background queue is added.
- Meal-favorite mutation uses a separate dependency-free page-memory recovery contract over the existing owner favorite-list read. Network, retryable service and unknown adapter outcomes freeze all favorite toggles behind one polite atomic receipt; explicit non-retryable refusal is terminal. PUT evidence requires the same food key plus an exact deep match of the submitted food snapshot and default serving, while DELETE evidence requires key absence. A mismatched current snapshot is divergent and an unchanged list restores only a later explicit-toggle boundary. Reconciliation updates the accepted favorite list but never the meal draft/source tab and never sends PUT/DELETE, polls, persists a command or creates a background queue.
- Optional-consent revocation has a separate dependency-free page-memory recovery contract over the current privacy overview. Network, retryable service and unknown adapter outcomes retain only the target purpose, close confirmation and freeze all custody actions behind the accepted inventory; explicit non-retryable refusal terminates the attempt. One explicit overview GET accepts only `revoked` as applied, treats `active` as not applied and missing/`never_granted` as divergent. The accepted current overview replaces the old inventory, but reconciled success never invents cleanup counts because only the lost POST response carried them. Read failure keeps the receipt; reconciliation never sends POST, polls, persists a purpose/request or creates a background queue.
- Owner-created exercise and food definition history routes apply the same 20/50 revision boundary and exact route/owner/anchor validation over existing descending indexes. Both editors request 10 at a time through one shared `DefinitionRevisionLedger`; exercise correction now exposes its audit evidence, food history is bounded, archived entries remain owner-readable and no definition value enters the cursor. The ledger reuses the page-memory history authority: unknown first reads stay distinct from successful empty, failed suffix reads retain a labeled frozen prefix and retry only the failed GET. The exact definition and unsaved correction remain editable, because audit-read failure does not revoke separately accepted register/mutation authority.
- Weekly-plan history now completes the same bounded revision family: strict 20-default/50-maximum pages use only plan UUID/revision, validate the path, owner and exact anchor, and continue below the immutable revision boundary over the existing descending index. Week Fold requests 10 decisions at a time, appends older generated/modified/accepted/skipped snapshots explicitly and keeps historical normalization, labels and freshness behavior unchanged. Its client continuation applies the shared audit failure taxonomy: a failed suffix retains and labels the accepted prefix, freezes the old cursor and exposes one focused retry. The current plan and independently accepted AI explanation ledger remain readable, successful-empty explanations receive an accepted-snapshot receipt and retry never regenerates the plan or calls the model.
- Browser code imports dependency-free contract constant subpaths instead of the CommonJS/Zod umbrella. The two public identity responses retain exact-key, URL, UUID, provider and offset-datetime checks through a small client guard while the shared Zod schemas remain the server/test authority. Bundle scanning fails if the removed full validation-runtime markers return.
- NestJS 11 modular API with Zod 4 contracts rendered into a committed OpenAPI 3.0 document.
- Provider-neutral users/identities plus provider-bound opaque Bearer sessions. WeChat exchanges a short-lived code server-side, namespaces `openid` by AppID and never persists `session_key`. H5 creates tab-scoped state/nonce/verifier values, derives PKCE S256, removes callback parameters before network work, consumes the transaction once, exchanges code + PKCE at the API, verifies remote JWKS/algorithm/issuer/audience/age/nonce, and stores only an issuer/subject digest. The development issuer is production-disabled; cross-provider account linking is intentionally absent.
- Independent pre-provisioned OIDC operator identities, least-privilege roles and opaque administrator sessions; remote JWKS/issuer/audience/age/nonce and one-time exchange are verified before issuance.
- Exact ticketed support lookup returns only lifecycle, aggregate and custody evidence. Administrator targets are HMAC references in a PostgreSQL-trigger-enforced append-only audit stream.
- Next.js 16 administrator BFF keeps the API credential in an HttpOnly cookie and renders the responsive Evidence Desk without user-content browsing or mutation controls.
- Transactional adult profile, goals, risk eligibility and immutable versioned consent events with optimistic revisions.
- The profile/goal client register treats unread, confirmed absence and an accepted response as distinct authority. Initial failure hides starter personal facts; stale refresh retains the accepted base and local draft only in page memory while PUT freezes. Current response revision and draft base remain separate, so changed-revision recovery never silently rebases edits. A 409 performs one GET reconciliation and requires explicit discard/load-latest before save; no profile/risk/consent draft, response, request or replay instruction is persisted or polled.
- Profile/goal save response-loss recovery freezes the exact submitted request/base revision and locks form controls only in page memory. Network, retryable service and unknown adapter outcomes replace save with one current-profile GET; explicit refusal is terminal. A first/advanced current revision succeeds only when the complete response-visible profile, goal, ordered constraints, risk flags and three consent versions match. Same revision/confirmed absence permits only a later explicit save, while changed/missing evidence enters the existing explicit discard/load-current flow without replacing local input. Reconciliation never PUTs, polls, persists a command or infers safety/consent state from revision alone.
- Transactional measurement create/replace/soft-delete with owner-only append-only snapshots, expected revisions and idempotent creation.
- An owner-scoped health insight endpoint accepts one exact metric code, reads only current non-deleted confirmed rows, calculates canonical-unit 7/30/90-day count/date/min/max/average evidence and returns up to 180 points with display/source/timezone/revision provenance. Candidates and unlike metrics never merge; correction/deletion recomputes without a rollup, and the client provides no diagnostic or goal verdict.
- Transactional relational workout aggregates with ordered exercises/sets, completed-only deterministic summaries, server-authoritative completion status, expected revisions, status-insensitive idempotent creation and immutable JSON snapshots.
- A versioned dependency-free starter exercise catalog plus owner-scoped custom definitions with searchable aliases, explicit tracking/equipment semantics, idempotent creation, optimistic correction, archive lifecycle and immutable definition revisions. Mutable action management now uses the lazy owner-register route shared with food definitions but dispatches to an action-specific view; the workout page refreshes active choices on return without remounting its draft. Every new workout snapshots the selected semantics, so later catalog edits cannot rewrite historical facts; legacy workout snapshots remain readable.
- An owner-scoped exercise insight endpoint groups only by exact stable key, counts sessions/metrics only from completed sets, calculates full 7/30/90-day windows in PostgreSQL and returns a bounded 180-point series with recorded snapshot fields, local date and workout revision. It reads corrected current rows, excludes soft deletions, persists no duplicate trend state and produces no progression prescription.
- Transactional nutrition aggregates with food/serving snapshots, canonical grams, deterministic kcal/P/C/F/fiber totals, owner favorites and immutable JSON revisions.
- A versioned starter food catalog plus owner-scoped custom definitions with searchable aliases, bounded per-100-g facts, required user-confirmed reference, idempotent creation, optimistic correction/archive and immutable definition revisions. The dedicated register manages mutable definitions; the meal page copies selected values into a draft/snapshot and refreshes active definitions on return. Privacy inventory/export v4/erasure cover active and archived rows/history, while photo candidates remain starter-bound.
- An owner-scoped nutrition insight endpoint generates exactly 90 requested-timezone local dates from current non-deleted meal/item snapshots, excludes facts later than its reference instant, keeps missing nutrients null, exposes fiber known/total item coverage and derives 7/30/90-day summaries without stored duplicate state. The dedicated Evidence Ribbon view labels recorded-day averages and provides no target, adherence or diet advice.
- Timezone-aware read-only Today projection with confirmed evidence, nullable recovery summary and 7/30/90-day totals. Its client projection has explicit initial-loading/ready/refreshing/initial-error/stale phases: only a successful read may render zero/empty evidence, while a failed refresh retains the last atomic dashboard/plan pair in page memory, labels it stale and permits one foreground retry without persistent cache or polling.
- Week Fold applies the same read-authority phases to one composed plan-list/workout/decision-history/AI-history snapshot. Initial failure cannot become `NO WEEK YET`; refresh failure retains one labeled revision/history view while every plan mutation, workout association and provider call remains frozen until explicit retry. The snapshot stays in page memory, exposes no raw server copy and adds no polling, persistent cache or background replay.
- The privacy custody desk applies the same five-phase read authority to its account inventory and consent ledger, but its first read is ordered: recover any pending local erasure receipt, then request the overview. Initial failure cannot become zero inventory or expose custody controls; a failed post-mutation refresh retains one labeled in-memory inventory while export, consent revocation and erasure preparation remain frozen. Back, profile editing and logout remain available, and no polling, persistent overview cache or background action is added.
- Versioned weekly plans with deterministic availability/load/equipment constraints, evidence snapshots, substitutions, optimistic decisions and immutable history. The list response adds a non-persisted server freshness projection (`current`, `evidence_changed`, `profile_changed`, `eligibility_blocked`, or defensive `onboarding_required`) from current profile/eligibility and one dashboard. `planning-impact-v1` fingerprints only missing/below-60/at-or-above-60 recovery bands, so workout/meal counters and same-band score changes remain no-ops. Permission literals and server rechecks close accept/modify/AI on material drift while preserving skip; regeneration keeps the plan ID and appends a new immutable revision. Historical snapshots are normalized at the read boundary, and AI context does not receive the new policy metadata.
- Explicit `plan_workout_links` keep plan and workout aggregates independent while binding their exact selected revisions. Composite owner foreign keys and partial active uniqueness prevent cross-user or one-to-many links; only an accepted, current plan session and current non-deleted workout may be linked. User unlink and workout soft deletion close rows with revision/reason/time instead of deleting them. Active projections expose original/current workout revisions in the Week Fold and Today, never infer adherence, and all rows join the owner export/erasure scope.
- Parameterized `pg` access to PostgreSQL 18.4 with transactional, checksum-protected SQL migrations.
- React Native for the later native App rather than forcing device integrations into the first client.
- NestJS modular monolith + PostgreSQL + Redis for business services.
- FastAPI worker with authenticated fixture/OpenAI provider adapters, strict structured output, bounded retry and health check. The v2 food-photo prompt treats image text as untrusted data and rejects instruction-dominant images rather than following or repeating their content.
- Review-only AI runs with explicit versioned consent, minimized context, prompt/model/validator provenance, owner-scoped idempotency, deterministic validation/fallback and exact plan-revision binding. Versioned v2 output safety performs NFKC normalization, removes format characters and compacts separators before checking medical/prescriptive/control-language policy; number grounding separately normalizes full-width and separated digits without rewriting stored prose. Every reservation stores a separately validated recovery result and database deadline beyond the worker timeout; runtime startup/interval reconciliation uses atomic `SKIP LOCKED` claims, while private operations routes expose aggregate health and a bounded manual pass without user content. An authenticated `GET /plans/weekly/:planId/explanation-request` read uses the original idempotency key, reconciles an already-expired row to its deterministic fallback, returns only pending metadata or the exact completed explanation, conceals foreign/absent requests and sets `private, no-store` caching. The existing owner-only history read now carries the same no-store boundary; a dedicated lazy ledger reloads current plan authority, separates current/frozen/history revisions, reveals at most five more retained runs per action and never calls the provider or exposes its internal identifier.
- Revocable food-photo proposals with per-request consent, signed upload/preview, Sharp metadata stripping, 24-hour expiry, catalog-bound and display-copy validation, durable deletion paths and confirmation into an unsaved draft only. Historical v1 provenance remains readable while new worker calls require prompt/validator v2.
- Privacy-first progress photos with declared front/side/back view, separate analysis/retention consent, Sharp metadata stripping, four deterministic capture-quality checks, 24-hour analysis-only expiry, retained same-view onion-skin comparison, explicit deletion and no posture/body-composition inference.
- Private S3-compatible object storage with checksummed/conditional writes, production-required SSE, purpose-scoped `food`/`progress` keys, legacy deletion compatibility and local pinned MinIO.
- PostgreSQL durable data-operation jobs with transactional enqueue, atomic `SKIP LOCKED` claims, leases, exponential retry, attempts, dead-letter state and aggregate operations evidence.
- User-owned privacy custody with inventory/export/revocation plus `durable-erasure-v2`: immediate access closure, status-token receipt, media/primary/provider/backup dispositions and cleared completed-subject fields.
- Recoverable account erasure uses a 15-minute single-use intent, stores only SHA-256 token hashes, persists the secret on the client before deletion and reuses it for a rate-limited no-store receipt lookup after the account session is closed.
- HMAC erasure ledger outside the database backup domain and a real `pg_dump → pg_restore → ledger replay` drill that removes a deleted user restored from an older backup.
- UUIDv4 request correlation, stable-route completion logs, a pre-authentication IP gate and post-authentication policy limits backed by HMAC-keyed Redis counters.
- Durable erasure ledger v2 stores unlinkable provider-identity HMAC references outside the backup domain; restore replay recreates identity suppressions before traffic, while legacy v1 entries derive suppressions from identities found in the isolated backup.
- Dependency-free liveness, PostgreSQL+Redis+object-storage readiness and independently token-protected bounded metrics/job evidence; business traffic fails closed when Redis is unavailable and durable deletion retries storage failure.
- Shared contracts, domain rules, and design tokens in a pnpm monorepo.
- Pinned-base, non-root OCI images for API, administrator and AI; API uses a self-contained pnpm deployment closure and administrator uses Next.js standalone output.
- A disposable pinned Compose topology runs a one-shot migration before traffic and black-box verifies image health, dependency readiness, request correlation and administrator security headers. GitHub Actions defines full quality/image smoke plus multi-architecture GHCR publication with provenance.
- The Nest API has typed `runtime` and `metadata` startup policies: production/integration retain dependency checks and maintenance workers, while OpenAPI/CORS inspection and contract generation assemble the real graph without external startup I/O.
- Both GitHub workflows pin every external action to a reviewed full commit recorded in `myfitness-github-actions-lock/v1`; an offline discovery test rejects mutable/unknown refs and weekly Dependabot proposals expose upgrades without automatic trust.
- A dependency-free `myfitness-release-qualification/v1` gate resolves lightweight or annotated remote tags, proves the exact commit remains in current `main` and selects that SHA's successful `main` push CI before registry login or client packaging. The strict record is rechecked against the release workflow and retained as an immutable Release asset.
- A dependency-free `myfitness-release/v1` control plane binds API, administrator and AI digests to one qualified SemVer tag, full source revision and workflow run; the tag workflow rejects mixed fragments and publishes checksummed, non-overwritable GitHub Release assets.
- A dependency-free `myfitness-client-release/v1` control plane packages sorted, fixed-metadata USTAR H5/WeApp roots, verifies canonical bytes and tree digests, and binds both platforms to one source/workflow/API base. H5 is a `candidate` with OIDC identity and required callback assets; WeApp is a `candidate` with WeChat identity. Both remain controlled-preview artifacts until real identity and custody proof.
- `myfitness-managed-environment/v1` plus `myfitness-deployment-admission/v2` require explicit account/budget, distinct public origins, secret-manager references, data-custody owners, telemetry and AI policy evidence before binding both verified release planes to migration/private-service/canary order, guarded client delivery and an explicit rollback mode.

## Current risks

| Risk                                                                                          | Level  | Mitigation / next evidence                                                                                                                                            |
| --------------------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Production audit retains nine moderate Taro build-chain advisories                            | Medium | Remove through a supported Taro/build-chain upgrade; rerun graph, dual-build and E2E proof                                                                            |
| Offline admission cannot prove external references exist or were genuinely approved           | High   | Create/dereference the dossier inside a protected change system; never deploy from local success alone                                                                |
| The published `v0.1.0-rc.1` predates client release and source-qualification assets           | Medium | Set an approved client API URL and publish/verify a new qualified tag; never mutate the existing release                                                              |
| Pinned GitHub Actions can age after their reviewed upstream releases                          | Medium | Review weekly Dependabot proposals, upstream tags/source and the synchronized lock; require complete CI                                                               |
| Scope may expand before the recording loop is proven                                          | High   | Enforce MVP exclusions and one-scope iteration archives                                                                                                               |
| Food-photo portion estimates can be misleading                                                | High   | Catalog-bound ranges/confidence, user edit, no auto-write; broaden real-image evaluation                                                                              |
| Progress photos can be over-interpreted or retained longer than expected                      | High   | Capture-only checks/no body score, separate retention consent, scoped deletion; real-user/policy review                                                               |
| AI may generate unsafe training or diet changes                                               | High   | Deterministic constraints and validators precede model output                                                                                                         |
| Production AI retention/region/cost/quality are unverified                                    | High   | Keep fixture default and provider receipts `policy_bound`; require approved real canary                                                                               |
| Twenty-three deterministic AI cases do not establish real-world safety                        | High   | Add expert-reviewed real/obfuscated/injection image and text cases plus slice thresholds                                                                              |
| Local MinIO does not prove production object controls                                         | High   | Configure cloud bucket/KMS/IAM/lifecycle/versioning/replication and exercise outage/restore                                                                           |
| Domestic Android health data is fragmented                                                    | Medium | Defer device sync; start HealthKit/Health Connect/Huawei feasibility after retention gate                                                                             |
| Brand name “衡迹” is unverified                                                               | Medium | Treat as working name; perform trademark/domain review before public launch                                                                                           |
| H5 entry remains 312 KiB (above webpack's 244 KiB advisory); largest async route is 200.4 KiB | Medium | Enforced 320,000/206,000-byte entry/async, 2,805,000 H5, 1,067,000 WeApp and 56,100 page budgets bind growth; keep optional workflows behind measured lazy boundaries |
| Taro emits non-blocking webpack cache serialization warnings                                  | Low    | Track upstream/package compatibility; clean builds and artifacts currently pass                                                                                       |
| WeChat lacks real device/domain proof; H5 OIDC has only browser/provider-double proof         | High   | Exercise both real adapters/domains, callback hosting and explicit re-registration policy                                                                             |
| Enterprise operator OIDC tenant/client and access owner are absent                            | High   | Select provider; exercise provisioning, recertification, disablement and shared login                                                                                 |
| Administrator audit lacks independent retention/export and alerts                             | High   | Define retention/owner; ship immutable copy and alert review before real operator access                                                                              |
| Local restore replay works; backup/provider operations are unowned                            | High   | Automate backup/retention, independently retain ledger and approve provider controls                                                                                  |
| A receipt bearer secret remains in client application storage until explicit removal          | Medium | Review secure platform storage/shared-device handling and expiry policy before closed beta                                                                            |
| Dead-letter recovery has no alert owner or safe service endpoint                              | High   | Centralize alerts; require audited exact-job runbook until a least-privilege tool exists                                                                              |
| Process metrics are not centrally scraped and alerts have no owner                            | High   | Deploy private aggregation, dashboards, paging and named incident ownership before beta                                                                               |
| Rate limits use uncalibrated fixed windows and exact proxy topology                           | Medium | Load-test policy boundaries and verify `TRUST_PROXY_HOPS` in the shared environment                                                                                   |
| Starter exercise catalog is small demonstration content, not a localized release corpus       | Medium | Review license, attribution, localization, media rights and content quality before importing candidates                                                               |
| User-authored exercise names/equipment can be incomplete or misleading                        | Medium | Keep them visibly user-owned facts; do not treat directory content as coaching or safety validation                                                                   |
| Starter food values are demonstration data, not release catalog                               | High   | Select licensed/localized versioned provider and attribution before beta                                                                                              |
| User-authored food nutrients/references can be incomplete or wrong                            | High   | Keep them visibly owner-confirmed, snapshot provenance, allow correction and never treat as verified/advice                                                           |
| Energy/macro UI can be harmful for eating-disorder risk                                       | High   | Maintain scope exclusion; add screening/content review before adaptive nutrition planning                                                                             |
| Nutrition trends can be mistaken for complete intake or a target                              | High   | Keep missing days/nutrients nullable, expose label coverage and prohibit goals/adherence/advice                                                                       |
| Deterministic-v1 is explainable but not clinically validated                                  | High   | Keep general-guidance claims; add offline evaluation and expert/content review                                                                                        |
| Explicit links remain observations and do not validate/adapt future workload or adherence     | Medium | Keep adaptation off; define an evaluated policy and expert review before links affect future plans                                                                    |
| Exercise trends can be mistaken for technique quality or a progression instruction            | High   | Keep completed evidence, units and revisions explicit; do not score quality or auto-prescribe load                                                                    |
| Health metric statistics can be mistaken for normal ranges, improvement or diagnosis          | High   | Keep exact metrics/units/provenance; exclude candidates and prohibit bands, verdicts and recommendations                                                              |
| Sensitive local drafts rely on platform application-storage and shared-device behavior        | Medium | 24-hour expiry, owner/schema/size gates and clear rules; review platform secure storage/backup before beta                                                            |
| Occurrence conversion depends on browser/Mini Program `Intl` timezone data                    | Medium | Deterministic gap/overlap tests pass; verify supported real devices/timezones before closed beta                                                                      |
| Automated keyboard/focus proof does not establish real screen-reader or device behavior       | Medium | Run VoiceOver, TalkBack and WeChat-device audits before closed beta; keep claims limited to tested H5 semantics                                                       |
| Browser response-loss simulations do not prove physical radio loss or WeChat device behavior  | Medium | Run real-device/network-transition checks; retain same-key/reconcile/terminal boundaries and never background-replay sensitive operations                             |

## Quality gates

The MVP cannot enter public beta until all of the following are reproducible:

- A new user can complete onboarding, record body/training/nutrition/recovery data, view trends, receive a plan, and delete/export their data.
- AI-derived values never silently become confirmed records.
- Plan output passes schema, training-load, energy-intake, and risk-phrase validation.
- Permissions, account deletion, photo retention, audit logging, backups, and incident rollback are exercised.
- CI passes formatting, linting, type checks, unit tests, integration tests, zero critical/high production dependency audit, and production builds.
- H5 and WeApp production trees pass checked-in entry/route/vendor/page/total budgets and the client validation-runtime regression scan.

## Primary next step

Iteration 88: bind the portable-export action to the privacy page's current lifecycle authority. Acceptance should prevent a late validated artifact from downloading, saving or publishing success after unmount or accepted-overview authority loss; revoke any created H5 Blob URL; allow a later explicit action to start fresh; and retain the existing schema/media/size verification, no-content-in-state and no-background-replay boundaries. Owner-operated cloud account/budget, domain/TLS, real WeChat/OIDC, production object storage/KMS, data-custody owners, centralized telemetry, policy/filing decisions, real WeApp file-system proof and any approved paid-provider canary remain mandatory external inputs but are parked until the user supplies them.
