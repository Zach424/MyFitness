# iLens 能力与迁移矩阵 v0.1

最后审阅：2026-08-12

## 1. 使用方式

本文把 `dataModel.md` 的目标模型与仓库实际实现逐项对照，用于决定复用、重组、扩展和新建的边界。它是迁移导航，不是已实现功能清单；实际能力以 `docs/product/IMPLEMENTED_PRD.md` 和可复现验证为准。

迁移类型：

- **直接复用**：当前领域语义和数据边界已经足够，只补入口或组合视图。
- **界面重组**：底层能力可复用，但页面结构不符合 iLens 的任务流。
- **模型扩展**：沿既有聚合、修订和来源规则增加受控字段或投影。
- **新建领域**：当前没有可承载目标语义的聚合，不能用 UI 命名冒充。

## 2. 领域能力矩阵

| 目标领域           | 当前实现证据                                                                               | 当前可复用部分                                               | 主要缺口                                                                                   | 迁移类型                     | 关键依赖                                              | 首个可验证出口                                               |
| ------------------ | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------ | ---------------------------- | ----------------------------------------------------- | ------------------------------------------------------------ |
| Personal Profile   | 引导资料、目标及不可变目标修订；`apps/api/src/onboarding`、`packages/contracts`            | owner 隔离、时区、单位制、训练经验、器械、饮食偏好           | 生日/年龄事实、训练年限、职业活动、明确限制与偏好来源仍不完整                              | 模型扩展                     | Evidence 语义、隐私导出/删除                          | 旧资料无损读取，新字段可修订且来源明确                       |
| Goal               | 当前目标、目标修订与 Personal Model 目标证据                                               | 目标 revision、精确发生时间、当前指针                        | 多目标优先级、生命周期和阶段性目标不足                                                     | 模型扩展                     | Profile、Plan v2                                      | 目标历史与当前计划能引用精确修订                             |
| Body Metrics       | `health_records`、修订历史、九个指标、单位/来源/发生时间                                   | 记录、更正、删除、历史、趋势、候选与确认边界                 | 身高历史、BMI、骨骼肌、内脏脂肪、基础代谢、节段数据和完整度不足                            | 模型扩展                     | 指标注册表、Body Assessment                           | 新旧指标共享同一记录生命周期，单位和范围由契约固定           |
| Body Assessment    | 无检测事件或报告聚合                                                                       | 食物照片流程可复用预约、私有上传、候选、确认、删除模式       | 检测事件、设备/机构、原始报告、提取候选、批量确认及指标关联全部缺失                        | 新建领域                     | Body Metrics、Evidence、私有媒体、AI 校验             | 一份报告只能产生候选；本人逐项确认后才写身体事实             |
| Body State         | 恢复指标、确定性恢复估计、个人状态证据账本                                                 | 睡眠、精力、压力、酸痛的来源与时间，Unknown/冲突表达         | 全身状态缺少统一快照和与训练负荷的清晰分层                                                 | 模型扩展                     | Body Metrics、Training、Evidence                      | 首页只展示可追溯状态，不把估计冒充测量值                     |
| Muscle Model       | v1 已固定 5 区域、26 个稳定 ID、层级、前后视图和版本；动作目录已消费 25 个可映射项         | 严格 Zod 契约、常量子路径、映射数据库约束与训练快照          | 词表升级兼容、内容专家审阅、训练量权重、人体 SVG 和肌群状态尚未实现                        | 新建领域，基础与首个消费完成 | 是多个下游领域的基础                                  | 保持 v1 历史解释不变，后续聚合只能消费有版本快照             |
| Exercise ↔ Muscle  | 9 个 starter 和 owner 自定义动作返回 mapped/unmapped；自定义关系可修订、导出并进入训练快照 | starter/custom 动作、owner、revision、显式来源与旧客户端兼容 | 当前客户端没有映射编辑界面；训练输入复制选择时来源但服务端尚不重新匹配目录，内容未专家审阅 | 模型扩展，持久化/API 完成    | Muscle Model                                          | 目录更正不改写旧 workout，未知与非法 aggregate 失败关闭      |
| Muscle State       | 仅有全身酸痛，没有肌群级状态                                                               | 恢复记录生命周期和训练组事实                                 | 肌群疲劳、酸痛、疼痛、不确定性、时间窗口和禁忌边界缺失                                     | 新建领域                     | Muscle Model、Exercise ↔ Muscle、Training、Body State | 肌群状态区分本人记录与系统估计，疼痛不触发医疗判断           |
| Training           | 完整训练、动作、组、修订、幂等、计划关联；新训练可保存选择时肌群关系快照                   | 绝大多数事实模型、发生时间、RPE、负荷、次数、距离、完成状态  | 未计划训练表达较弱，缺少执行模式、肌群聚合和模板化快捷记录                                 | 直接复用 + 模型扩展          | Muscle Model、Plan v2                                 | 旧日志无损，新日志不会随动作目录更正改变肌群解释             |
| Training Execution | 当前训练页以编辑和历史为主                                                                 | 新建/更正、响应丢失恢复、敏感草稿、动作目录                  | 进行中课次、组间计时、逐组完成、上次表现提示和中断恢复体验不足                             | 界面重组                     | Training、Exercise ↔ Muscle                           | 手机单手完成一节力量训练，刷新/离线不产生重复记录            |
| Performance        | 动作洞察提供次数、组数、总量和时间序列                                                     | 已完成训练事实、7/30/90 日窗口、动作身份                     | 最大重量、最佳组、次数 PR、e1RM、PR 事件与算法版本缺失                                     | 新建领域                     | Training、动作身份；肌群只用于聚合                    | 同一完成组可确定性复算，e1RM 始终标为估算而非实测 1RM        |
| Nutrition          | 餐食、条目、宏量营养、目录、收藏、照片候选和趋势较成熟                                     | 当前记录/修订/来源/确认和按日汇总                            | 计划目标、餐次计划关联、完整度与微量营养仅有局部覆盖                                       | 直接复用 + 模型扩展          | Plan v2、Evidence                                     | 记录事实不变，计划目标与实际摄入分栏显示                     |
| Plan               | 当前 `weekly_plans` 是单周生成/接受/修改/跳过聚合                                          | 周计划修订、证据、计划课次关联、结果回看                     | 长期计划、周期、active/paused/replaced/completed/archived 生命周期、草稿/当前/历史中心缺失 | 新建 Plan v2；旧计划只读兼容 | Profile、Goal、Training、Performance、Body State      | 新旧计划并存读取，迁移前不覆盖既有周计划历史                 |
| Evidence           | 各领域已有来源、时间、revision、幂等和历史；Personal Model 有两类精确证据                  | 强 owner 边界、不可变修订、候选/确认、精确外键模式           | 缺少跨领域统一信封、完整度、报告资产和更多 Personal Model 证据类型                         | 横向对齐；不先造万能表       | 每个新增领域共同执行                                  | 每个 AI/派生结果可回到精确 owner、聚合、revision、时间和状态 |
| Personal Model     | 八种 item kind、revision、反馈、证据、三种 claim 和三种本人校准已实现                      | 持久内核、来源资格、代际、当前视图、精确反馈                 | 只消费 goal/workout；缺 body/performance/nutrition claim、周回顾、公开 lineage 和导出      | 直接复用 + 后置扩展          | Body、Performance、Plan、Evidence 成熟后              | 新 claim 逐个建立覆盖门槛、反证、Unknown 与本人校准          |
| AI Closed Loop     | AI 解释、食物照片候选、安全校验器和离线评估存在                                            | 模型网关边界、候选确认模式、确定性安全验证                   | 报告解析、训练/饮食规划编排、计划执行反馈和跨域总结缺失                                    | 模型扩展 + 新编排            | 全部事实域和 Plan v2                                  | AI 只提候选/草案，Schema 与安全规则通过后由本人确认          |

## 3. 页面迁移矩阵

| iLens 目标入口  | 当前页面来源                                                      | 迁移策略                                                                                 | 不应做的事                                  |
| --------------- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------- |
| 首页            | `pages/index`、恢复状态、周计划、营养汇总                         | 重组为今日训练、饮食、恢复、身体人体图和待确认五个任务卡；先读现有 API，再逐步接入新领域 | 不先复制一套“首页专用事实表”                |
| 训练 / 执行     | `pages/workouts`、动作目录                                        | 保留日志能力，拆出进行中执行状态和明确保存边界                                           | 不让本地计时器成为权威训练事实              |
| 训练 / 计划中心 | `pages/plans`、`pages/plan-outcome`                               | 旧周计划作为兼容视图；Plan v2 成熟后提供当前、草稿、历史和周期                           | 不原地改写旧状态枚举并丢失历史              |
| 训练 / 训练分析 | `pages/exercise-insights`、训练历史                               | 组合训练行为、动作表现、PR 与肌群分布                                                    | 不把训练量或 e1RM 描述为技术质量或医疗建议  |
| 身体档案        | `pages/records`、`pages/health-insights`、`pages/progress-photos` | 重组为身体指标、体测事件、趋势和私有照片；历史事实继续走原聚合                           | 不把 AI 图片估计直接写成确认指标            |
| 饮食            | `pages/nutrition`、`nutrition-insights`、`food-catalog`、照片流程 | 直接复用，新增计划目标与实际分栏                                                         | 不因计划存在而修改已记录餐食                |
| iLens 认识      | `pages/personal-model`、`ai-explanations`                         | 保留当前三主题和本人校准，待基础事实成熟后扩充周回顾                                     | 不先生成宽泛心理画像或任意 JSON 标签        |
| 我的 / 隐私     | 引导、隐私页、数据导出与删除                                      | 直接复用并纳入新增领域的同意、保留、导出、纠正和删除                                     | 不把新报告/肌群状态排除在用户数据所有权之外 |

## 4. 可直接复用的工程资产

1. **共享契约和严格运行时**：`packages/contracts`、TypeScript strict 和 OpenAPI 生成可继续作为 API-first 边界。
2. **修订与所有者隔离**：健康、训练、餐食、计划、目录和 Personal Model 已证明 owner、revision、history、幂等与响应丢失恢复模式。
3. **候选后确认媒体流**：食物照片的私有预约、分析候选、本人确认和删除流程可作为体测报告导入的流程参考，但不能复用其内容 Schema。
4. **确定性洞察**：现有动作、健康、饮食和恢复投影适合作为 Performance 与首页聚合的实现模板。
5. **隐私生命周期**：同意、便携导出、账号删除、私有对象处置和审计边界必须覆盖每个新增领域。
6. **Personal Model 内核**：严格 claim、证据资格、代际和反馈机制保留；在新事实域可验证前，不扩张成通用用户画像。

## 5. 跨域数据规则

- 所有可变化事实必须携带 `occurredAt`、系统接收时间、来源、owner 和 revision；数据库精确时间不得经过 JavaScript 毫秒精度往返后再参与资格比较。
- AI 提取、照片估计和计算结果默认是 `candidate` 或派生读模型；只有本人确认或确定性规则允许的投影才能进入相应事实域。
- 完整度与 Unknown 分开：缺少数据不能等于零，低覆盖不能生成高置信结论。
- `primaryMuscles`、`secondaryMuscles`、指标代码、性能算法和计划策略都要有版本，避免历史解释随词表更新静默变化。
- e1RM、肌群疲劳和身体状态估计必须与本人记录、设备测量和机构报告使用不同的来源标签。
- 新领域验收同时包含权限、保留、导出、纠正和删除；只完成主流程不算完成。

## 6. 依赖主链

```text
Muscle Model → Exercise-Muscle → Muscle State / Training Analysis / Human SVG
Body Metric Registry → Body Assessment + Evidence → Body Profile / AI Report Import
Training Facts → Performance → Training Analysis → Plan v2
Profile + Goal + Body State + Performance + Nutrition → Plan v2
All confirmed facts + exact revisions → Personal Model → AI closed loop
```

详细阶段、每轮边界和退出证据见 `docs/product/ROADMAP.md`；架构取舍见 ADR-0197。
